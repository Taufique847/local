Bilkul. **M15 ke baad M16 ko “Customer 360 / Customer Relationship & Interaction Timeline Engine”** banana logical hai.

M15 ne calls ko centralize kiya; M16 un calls, leads, appointments aur communications ko **ek unified customer profile** me connect karega. Isse HVAC business owner ko ek customer ka complete history ek jagah milega.

# MASTER PROMPT — M16

## CUSTOMER 360 & RELATIONSHIP MANAGEMENT ENGINE

You are a senior production SaaS architect and full-stack engineer working on an existing multi-tenant AI Employee SaaS platform for US HVAC/home-service businesses.

The platform currently contains modules through M15.

You are now implementing:

# M16 — CUSTOMER 360 & RELATIONSHIP MANAGEMENT ENGINE

The objective of M16 is to create a reliable, multi-tenant Customer 360 system that gives a business a complete view of each customer and their relationship with the business.

This module must integrate with existing:

* Authentication
* RBAC
* Multi-tenancy
* M10 AI Voice Engine
* M11 Tool Calling
* M11.2 Reliability Layer
* M12 Lead Automation
* M13 Appointment Engine
* M14 Communication/SMS Engine
* M15 Call Logs, Transcripts & Analytics

DO NOT rebuild existing modules.

---

# 1. PRIMARY OBJECTIVE

Build a professional Customer 360 system where a business can open one customer and see:

* customer profile
* contact information
* service addresses
* all calls
* call transcripts
* leads
* appointments
* SMS communications
* customer activity
* current open opportunities
* upcoming appointments
* past service interactions
* last interaction
* next scheduled interaction
* customer tags
* customer notes
* customer status
* basic relationship statistics

The goal is:

CUSTOMER

↓

CALLS

↓

LEADS

↓

APPOINTMENTS

↓

COMMUNICATIONS

↓

ACTIVITY TIMELINE

Everything should be connected.

---

# 2. FIRST ACTION — INSPECT EXISTING SYSTEM

Before writing code, inspect the complete project.

Inspect:

M12 Customer references

M12 Lead model

M13 Appointment model

M14 Communication model

M15 CallLog model

M15 Transcript model

existing User model

existing Business model

existing tenant middleware

existing RBAC

existing authentication

existing event system

existing logger

existing API conventions

existing frontend design system

existing customer-related APIs

existing customer components

existing phone normalization

existing address schema

existing pagination

existing search implementation

Do not create duplicate functionality.

If a Customer model already exists:

EXTEND it safely.

Do NOT create:

Customer.js

CustomerModel.js

Client.js

CustomerProfile.js

multiple competing customer models

unless the architecture genuinely requires them.

There must be one authoritative customer entity.

---

# 3. CUSTOMER AS THE CENTRAL ENTITY

M16 should establish Customer as the central relationship entity.

Conceptually:

Business
│
├── Customer
│   ├── Calls
│   ├── Leads
│   ├── Appointments
│   ├── Communications
│   ├── Notes
│   └── Activities
│
└── Other business data

A customer can have:

0 or many calls

0 or many leads

0 or many appointments

0 or many communications

0 or many notes

0 or many activities

---

# 4. MULTI-TENANCY

This is NON-NEGOTIABLE.

Every customer must belong to exactly one business/tenant.

Required:

businessId

Never trust:

businessId from frontend

businessId from AI

businessId from request body

businessId from arbitrary query parameters

Resolve businessId from authenticated context.

Every Customer query must include tenant scope.

Example:

Authenticated User
→ Business Membership
→ businessId
→ Customer query

Business A must NEVER access Business B customers.

---

# 5. CUSTOMER MODEL

Create or safely extend the existing Customer model.

Recommended structure:

Customer:

* businessId
* firstName
* lastName
* fullName
* phone
* normalizedPhone
* email
* status
* preferredContactMethod
* notes
* tags
* addresses
* source
* firstContactAt
* lastContactAt
* lastCallAt
* lastAppointmentAt
* nextAppointmentAt
* metadata
* createdAt
* updatedAt

Do not duplicate data unnecessarily.

For derived information such as:

appointment count

call count

last interaction

calculate from source records or maintain safely updated aggregates.

---

# 6. CUSTOMER STATUS

Use controlled values.

Suggested:

ACTIVE
INACTIVE
ARCHIVED

Do not create excessive statuses.

Status must have documented semantics.

Example:

ACTIVE
Customer currently has an ongoing relationship.

INACTIVE
No recent active interaction but customer remains in database.

ARCHIVED
Business intentionally archived customer.

Do not hard-delete customers as normal UI behavior.

---

# 7. CONTACT METHODS

Use controlled enum:

SMS
PHONE
EMAIL

If the business has not provided an email:

do not display fake values.

If the customer has opted out of SMS:

respect M14 communication preferences.

M16 must NEVER override M14 consent/opt-out rules.

---

# 8. PHONE NORMALIZATION

Reuse the existing phone normalization implementation.

There must NOT be two competing phone normalization systems.

Store:

phone

normalizedPhone

where appropriate.

Use normalizedPhone for matching.

Do not use phone area code to infer timezone.

---

# 9. CUSTOMER DEDUPLICATION

Customer duplication is a major requirement.

The system should detect likely duplicate customers.

Primary matching signals may include:

* normalized phone
* email
* existing customer identifier

Example:

Customer A:
John Smith
+1 555 123 4567

New inbound call:
+1 555 123 4567

→ resolve existing customer.

Do NOT create another John Smith customer automatically.

---

# 10. DUPLICATE CUSTOMER SAFETY

If matching confidence is ambiguous:

do not automatically merge.

Example:

Same phone
but different names

→ flag potential duplicate / use safe resolution.

Never perform destructive merging without explicit authorization.

---

# 11. CUSTOMER CREATION

Customer creation can originate from:

AI voice call

manual dashboard

lead creation

appointment workflow

import if existing system supports imports

Do not create separate customer records for every event.

Use a central CustomerService.

Example:

resolveOrCreateCustomer()

The exact implementation should follow existing architecture.

---

# 12. AI CUSTOMER RESOLUTION

During a call:

Incoming phone
↓
normalize phone
↓
search tenant customers
↓
existing customer found?
YES → attach call
NO → create customer
↓
continue conversation

AI itself should NOT directly decide customer IDs.

Backend resolves the customer.

The AI may provide captured customer information, but the backend remains authoritative.

---

# 13. CUSTOMER ID SECURITY

Never allow AI to say:

"customerId = another tenant's ID."

The backend must verify:

customer.businessId === authenticated/tool-context businessId

Every customer lookup must be tenant scoped.

---

# 14. CUSTOMER ADDRESSES

HVAC customers may have multiple service addresses.

Support:

addresses: [
{
addressId,
label,
line1,
line2,
city,
state,
postalCode,
country,
isPrimary
}
]

Possible labels:

HOME
OFFICE
RENTAL
OTHER

Do not assume every customer has only one address.

---

# 15. PRIMARY ADDRESS

Exactly one address may be primary.

If the customer has only one:

that address can be primary.

When changing primary:

unset previous primary.

Never allow multiple primary addresses accidentally.

---

# 16. SERVICE ADDRESS VS CUSTOMER ADDRESS

Important:

Customer profile address
and
Appointment service address

are not necessarily identical.

M13 appointment remains the source of truth for where that appointment occurs.

Do not retroactively change old appointments when customer address changes.

Example:

Customer moves from:

123 Main Street

to:

456 Oak Avenue

Old appointment:
123 Main Street

must remain unchanged.

New appointment:
456 Oak Avenue

---

# 17. CUSTOMER NOTES

Support business notes.

Example:

"Customer prefers afternoon appointments."

"Gate code provided."

"Has two HVAC units."

Notes must be:

* tenant scoped
* permission controlled
* timestamped
* attributed to user/system

Do not overwrite history blindly.

---

# 18. CUSTOMER NOTE MODEL

Prefer a separate CustomerNote model if the existing architecture supports it.

Fields:

* businessId
* customerId
* authorId
* authorType
* content
* createdAt
* updatedAt

authorType:

USER
SYSTEM
AI

AI-generated notes must be clearly identified.

Do not store hidden AI reasoning.

---

# 19. NOTE SECURITY

Customer notes can contain sensitive business information.

Do not expose them:

* to unrelated tenants
* to unauthorized users
* in public APIs
* in logs

Respect RBAC.

---

# 20. CUSTOMER TAGS

Support lightweight tags.

Examples:

VIP
REPEAT_CUSTOMER
MAINTENANCE
INSTALLATION
COMMERCIAL
RESIDENTIAL
FOLLOW_UP
WARRANTY

Tags should be controlled/sanitized.

Do not allow arbitrary dangerous content.

If the project already has a tagging system:

reuse it.

---

# 21. CUSTOMER SOURCE

Possible sources:

AI_VOICE
PHONE
WEB
MANUAL
REFERRAL
IMPORT
OTHER

Use existing lead/source conventions where appropriate.

Do not create conflicting source enums.

---

# 22. CUSTOMER TIMELINE

This is one of the most important M16 features.

Create a unified customer activity timeline.

Example:

SEP 18
10:31 AM
Incoming call

10:32 AM
Lead created

10:34 AM
Appointment booked

10:35 AM
Confirmation SMS sent

SEP 19
2:00 PM
Appointment started

2:55 PM
Appointment completed

SEP 20
9:00 AM
Follow-up SMS sent

The timeline should combine factual events from:

M15 calls

M12 leads

M13 appointments

M14 communications

customer notes

future modules

---

# 23. TIMELINE EVENT TYPES

Possible:

CALL_STARTED
CALL_COMPLETED
CALL_MISSED

LEAD_CREATED
LEAD_UPDATED
LEAD_QUALIFIED
LEAD_STATUS_CHANGED

APPOINTMENT_CREATED
APPOINTMENT_CONFIRMED
APPOINTMENT_RESCHEDULED
APPOINTMENT_CANCELLED
APPOINTMENT_COMPLETED
APPOINTMENT_NO_SHOW

SMS_SENT
SMS_DELIVERED
SMS_FAILED

NOTE_CREATED
NOTE_UPDATED

CUSTOMER_CREATED
CUSTOMER_UPDATED

Use existing domain event types wherever available.

---

# 24. TIMELINE SOURCE OF TRUTH

Do not blindly duplicate every domain event into another permanent collection unless required.

Prefer assembling timeline from existing records/events.

If a dedicated CustomerActivity collection is necessary:

document exactly why.

Avoid unnecessary data duplication.

---

# 25. CUSTOMER ACTIVITY API

Implement:

GET /api/customers/:id/activity

Support:

page
limit
type
startDate
endDate

Return chronological events.

Pagination is required.

---

# 26. CUSTOMER LIST API

Implement or extend:

GET /api/customers

Support:

page
limit
search
status
tag
source
createdFrom
createdTo
lastContactFrom
lastContactTo

Tenant scope is mandatory.

---

# 27. CUSTOMER DETAIL API

Implement:

GET /api/customers/:id

Response should provide:

profile
contact
addresses
status
tags
summary metrics
recent activity
upcoming appointment
open leads

Do not return unlimited historical data in one response.

Use separate endpoints for large collections.

---

# 28. CUSTOMER UPDATE API

Implement:

PATCH /api/customers/:id

Allow safe updates to:

name
email
phone where allowed
status
preferredContactMethod
tags
addresses

Validate every field.

Do not allow:

businessId modification

internal IDs

createdAt manipulation

security metadata

---

# 29. CUSTOMER CREATION API

Implement:

POST /api/customers

Required validation should depend on existing business rules.

Possible minimum:

phone OR email

Prefer phone for HVAC call-originated customers.

Do not create customers with meaningless empty records.

---

# 30. CUSTOMER ARCHIVE

Provide:

POST /api/customers/:id/archive

or equivalent existing convention.

Archive rather than delete.

Before archive:

respect existing business rules.

Do not delete calls, leads, appointments, or communication history.

---

# 31. CUSTOMER RESTORE

If archived customers can be restored:

provide:

POST /api/customers/:id/restore

Only if consistent with existing architecture.

---

# 32. CUSTOMER SEARCH

Search should support:

name
phone
email

Optionally:

address

Do not search transcript text from Customer API.

M15 owns transcript search.

---

# 33. CUSTOMER SEARCH PERFORMANCE

Use appropriate indexes.

Potential:

{
businessId: 1,
normalizedPhone: 1
}

{
businessId: 1,
email: 1
}

{
businessId: 1,
createdAt: -1
}

Review actual query patterns before creating indexes.

---

# 34. CUSTOMER 360 PAGE

Create:

/dashboard/customers

and:

/dashboard/customers/[id]

---

# 35. CUSTOMER LIST UI

Display:

Customer
Phone
Email
Status
Last Contact
Next Appointment
Open Lead
Tags

Example:

John Smith
+1 (555) ***-1234
Active
Last contact: Today
Next appointment: Tomorrow 2:00 PM

Do not hardcode values.

---

# 36. CUSTOMER 360 DETAIL UI

Professional SaaS layout.

Header:

Customer Name
Status
Phone
Email
Actions

Possible actions:

Edit
Add Note
Archive
Call
SMS
View Appointment

Do not expose actions that the user's role cannot perform.

---

# 37. CUSTOMER OVERVIEW

Display cards:

Total Calls
Total Leads
Total Appointments
Last Contact
Next Appointment

Use real data.

---

# 38. CUSTOMER CONTACT CARD

Display:

Phone
Email
Preferred contact method

Buttons:

Call

SMS

Email

Only show actions supported by existing infrastructure.

SMS must go through M14.

Call must go through existing voice/telephony architecture.

Do NOT directly call Twilio from frontend.

---

# 39. CUSTOMER ADDRESS CARD

Show:

Primary service address

and:

Other addresses

Provide:

Add address
Edit address
Set primary

Do not modify historical appointment addresses.

---

# 40. CUSTOMER LEADS SECTION

Show related leads.

Columns:

Service
Status
Urgency
Created
Last updated

Click:

View Lead

Reuse M12 APIs.

Do not duplicate lead data.

---

# 41. CUSTOMER APPOINTMENTS SECTION

Show:

Upcoming
Past

Each row:

Appointment number
Service
Date
Time
Status

Click:

View Appointment

Reuse M13 source of truth.

---

# 42. CUSTOMER CALLS SECTION

Show recent calls.

Display:

Date
Direction
Duration
Outcome

Click:

View Call

Use M15 API.

Do not embed full transcripts in Customer page.

---

# 43. CUSTOMER COMMUNICATIONS

Show recent communications:

SMS
Status
Type
Date

Click:

View Communication

Use M14 source of truth.

---

# 44. CUSTOMER NOTES SECTION

Show:

Latest notes

Each note:

content
author
timestamp

Provide:

Add Note

Edit/Delete only according to RBAC.

---

# 45. CUSTOMER TIMELINE UI

The main timeline should be visually clear.

Example:

● Call completed
Sep 18 · 10:35 AM
AC repair inquiry

● Appointment booked
Sep 18 · 10:34 AM
Sep 19 · 2:00 PM

● SMS delivered
Sep 18 · 10:35 AM
Appointment confirmation

● Lead qualified
Sep 18 · 10:32 AM

Timeline should support:

filters
pagination
event icons
click-through navigation

---

# 46. QUICK ACTIONS

Customer detail page may provide:

Call Customer
Send SMS
Create Lead
Book Appointment
Add Note

IMPORTANT:

These actions must reuse existing services.

Example:

Send SMS
→ M14 CommunicationService

Create Lead
→ M12 LeadService

Book Appointment
→ M13 AppointmentService

Call
→ existing voice/telephony service

Never implement business logic separately in frontend.

---

# 47. AI CUSTOMER TOOL INTEGRATION

M16 may expose controlled tools such as:

find_customer
create_customer
update_customer

ONLY if these capabilities are required by the existing AI architecture.

Tool calls must go through:

M11
→ M11.2
→ CustomerService

AI cannot directly query MongoDB.

AI cannot choose businessId.

AI cannot bypass authorization.

---

# 48. FIND CUSTOMER TOOL

Conceptually:

find_customer({
phone,
email
})

Backend derives:

businessId

Then searches tenant-scoped records.

Return minimal necessary information.

Do not expose internal database fields.

---

# 49. CREATE CUSTOMER TOOL

Conceptually:

create_customer({
firstName,
lastName,
phone,
email,
address
})

Backend:

* validates input
* resolves business
* normalizes phone
* checks duplicates
* creates/reuses customer
* returns customer ID and safe summary

---

# 50. UPDATE CUSTOMER TOOL

AI should only update permitted fields.

Allowed examples:

name
email
address
preferred contact method

Do not allow AI to modify:

businessId
roles
billing
subscription
security settings
internal ownership
audit data

---

# 51. CUSTOMER MATCHING DURING CALL

Integrate M10 + M15.

Flow:

Incoming call
↓
Twilio number
↓
business resolved
↓
customer phone normalized
↓
CustomerService.findByPhone()
↓
customer found
↓
attach call to customer

If not found:

create customer only when sufficient information exists.

---

# 52. CUSTOMER MERGE

DO NOT build automatic destructive customer merging in M16.

If duplicates are detected:

display:

"Potential duplicate customer"

Provide a future-ready architecture.

Explicit merge can be a future module or separately authorized workflow.

---

# 53. CUSTOMER METRICS

Provide factual metrics:

totalCalls
answeredCalls
missedCalls
totalLeads
qualifiedLeads
totalAppointments
completedAppointments
cancelledAppointments
noShowAppointments
totalCommunications
lastContactAt
nextAppointmentAt

Do not infer:

customer value
customer loyalty
customer happiness
customer lifetime value

unless actual business data and a future analytics module support it.

---

# 54. LAST CONTACT

lastContactAt should be based on actual interactions.

Possible sources:

completed call
inbound SMS
outbound SMS
other supported communication

Document exactly which events update it.

Do not update lastContactAt for irrelevant internal events.

---

# 55. NEXT APPOINTMENT

nextAppointmentAt should be derived from active future appointments.

If there are no future appointments:

null.

Do not retain stale appointment values.

---

# 56. OPEN LEADS

Customer profile may show active/open leads.

Do not define lead ownership independently.

Use M12 status definitions.

---

# 57. CUSTOMER SUMMARY

The profile can have a short structured summary.

Example:

"Residential HVAC customer. Two previous service calls. One completed AC repair. Upcoming maintenance appointment scheduled."

This must be based on factual data.

Do not invent details.

---

# 58. AI-GENERATED CUSTOMER SUMMARY

If an LLM summary is used:

reuse existing provider abstraction.

Primary:
Azure OpenAI

Secondary:
OpenAI

Third:
Google Gemini

Model selection remains automatic/configuration-driven.

Do not expose provider credentials.

---

# 59. SUMMARY FAILURE

If AI summary fails:

Customer profile must still work.

Fallback:

show structured facts.

Do not block Customer 360 page on AI generation.

---

# 60. PRIVACY

Customer data may include:

name
phone
email
address
service information
conversation references

Protect all data.

Do not expose:

API keys
Twilio credentials
LLM credentials
internal prompts
hidden reasoning
internal security metadata

---

# 61. PII LOGGING

Do not log complete customer profiles unnecessarily.

Prefer:

businessId
customerId
correlationId

Mask phone numbers where appropriate.

---

# 62. RBAC

Respect existing roles.

Typical:

OWNER
ADMIN
MANAGER
STAFF

Follow the existing authorization architecture.

Do not create a second permissions system.

---

# 63. CUSTOMER DATA ACCESS

Customer detail endpoint must verify:

authenticated user
→ business membership
→ role
→ customer tenant ownership

Never rely on frontend routing security.

---

# 64. CUSTOMER EXPORT

DO NOT implement bulk customer export unless it already exists.

Export functionality can introduce significant privacy/security considerations.

Keep it outside M16 unless already required.

---

# 65. CUSTOMER IMPORT

DO NOT build a new CSV import system.

If existing import infrastructure exists:

integrate with it.

Otherwise defer.

---

# 66. CUSTOMER DELETE

Do not provide normal hard-delete.

Historical operational data must remain traceable.

Archive instead.

---

# 67. EVENT INTEGRATION

Use existing events:

lead.created
lead.updated
lead.status_changed

appointment.created
appointment.confirmed
appointment.rescheduled
appointment.cancelled
appointment.completed

call.started
call.completed
call.missed

communication.sent
communication.delivered
communication.failed

customer.created
customer.updated

Do not duplicate the event bus.

---

# 68. CUSTOMER TIMELINE CONSISTENCY

If an event occurs:

Example:

appointment.confirmed

Customer timeline must eventually show:

Appointment confirmed.

If event processing is asynchronous:

support eventual consistency.

Do not block the main appointment transaction unnecessarily.

---

# 69. IDEMPOTENCY

Use existing M11.2 reliability mechanisms.

Protect:

customer creation
event processing
timeline generation
AI customer tools

Example:

Same inbound call webhook twice

must NOT create:

Customer A
Customer B

if the same phone belongs to the same tenant.

---

# 70. CONCURRENCY

Customer creation can race.

Example:

Two simultaneous calls from the same new number.

Both request:

find customer

Both see:

not found

Both try:

create customer

This must not create duplicates.

Use:

* unique indexes where appropriate
* atomic upsert
* transaction where appropriate
* existing reliability mechanisms

Do not rely only on application-level "find then create."

---

# 71. UNIQUE INDEX SAFETY

Potential unique key:

businessId + normalizedPhone

But carefully consider:

* missing phone
* multiple customers sharing phone
* imported records
* historical data

Design based on the actual product rules.

Do not blindly make email globally unique.

Customer uniqueness is tenant scoped.

---

# 72. ADDRESS VALIDATION

Validate:

line1
city
state
postalCode
country

Do not require unnecessary external address APIs in M16.

If address verification is future functionality:

leave it extensible.

---

# 73. CUSTOMER API SECURITY

Prevent:

NoSQL injection

prototype pollution

arbitrary update fields

businessId injection

ObjectId injection

oversized payloads

unauthorized updates

Use allowlists for update fields.

---

# 74. FRONTEND SEARCH

Customer list search:

"Search customers by name, phone, or email..."

Debounce search.

Use server-side search.

Do not load all customers into browser.

---

# 75. CUSTOMER LIST FILTERS

Provide:

Status
Tags
Source
Last Contact
Created Date

If a filter isn't supported by backend:

do not fake it client-side.

---

# 76. SORTING

Support useful sorting:

Newest
Recently contacted
Upcoming appointment
Name

Validate sorting fields against an allowlist.

Never allow arbitrary MongoDB sort expressions from user input.

---

# 77. PAGINATION

Default:

25

Maximum:

100

Never permit unbounded queries.

Use the project's existing pagination implementation.

---

# 78. LOADING STATE

Customer pages must have:

Skeleton UI

not blank screen.

---

# 79. EMPTY STATE

If no customers:

"No customers yet"

Explain how customers are created:

"Customers are automatically created from calls and can also be added manually."

Only display this if consistent with actual implementation.

---

# 80. ERROR STATE

Example:

"Unable to load customer information."

Provide:

Retry

Do not expose backend stack traces.

---

# 81. MOBILE EXPERIENCE

Customer 360 must work on mobile.

Desktop:

sidebar
→ customer list
→ customer workspace

Mobile:

responsive navigation
→ customer list
→ customer detail

Use collapsible sections where necessary.

---

# 82. UI DESIGN

Do NOT build a childish/basic CRM.

The interface should feel like a professional modern SaaS application.

Use existing:

* design tokens
* typography
* cards
* tables
* badges
* modals
* drawers
* icons
* spacing
* animations
* accessibility patterns

Do not create another design system.

---

# 83. CUSTOMER DETAIL INFORMATION ARCHITECTURE

Recommended:

HEADER

Customer name
Status
Contact actions

↓

OVERVIEW CARDS

Calls
Leads
Appointments
Last Contact

↓

MAIN CONTENT

Activity Timeline
Calls
Leads
Appointments
Communications
Notes

↓

SIDEBAR

Contact
Addresses
Tags
Upcoming Appointment

Adjust according to existing design system.

---

# 84. ACCESSIBILITY

Support:

keyboard navigation

visible focus states

semantic buttons

ARIA labels where needed

sufficient contrast

reduced-motion compatibility

Do not make critical functionality hover-only.

---

# 85. API PERFORMANCE

Customer detail page should NOT trigger dozens of independent requests unnecessarily.

Design an efficient aggregation strategy.

Possible:

GET /api/customers/:id

for overview

Then lazy-load:

activity
calls
leads
appointments
communications

Do not fetch entire history automatically.

---

# 86. N+1 QUERY PREVENTION

Avoid:

customer list
→ query appointment separately for every customer
→ query calls separately for every customer
→ query leads separately for every customer

Use appropriate aggregation/population/batched queries.

Inspect actual performance.

---

# 87. CUSTOMER LIST PERFORMANCE

Customer list should remain fast with thousands of customers.

Use:

pagination

indexes

projection

efficient queries

Do not return:

full transcript
full call history
all communications

in the list endpoint.

---

# 88. CUSTOMER 360 PERFORMANCE

For a customer with hundreds/thousands of interactions:

Do not load all history at once.

Use:

pagination

lazy loading

server-side filtering

---

# 89. CUSTOMER ACTIVITY PAGINATION

Example:

GET /api/customers/:id/activity?page=1&limit=25

Return:

items
page
limit
total
hasNextPage

Follow existing response conventions.

---

# 90. TESTING — CUSTOMER

Test:

customer creation

customer retrieval

customer update

customer archive

customer restore if implemented

---

# 91. TESTING — DUPLICATES

Test:

same phone

same email

simultaneous creation

duplicate inbound calls

imported customer edge cases

---

# 92. TESTING — TENANT ISOLATION

Business A:

Customer A

Business B:

Customer B

Verify:

A cannot access B.

B cannot access A.

Test:

GET
PATCH
activity
calls
leads
appointments
communications

---

# 93. TESTING — RELATIONSHIPS

Test:

customer → calls

customer → leads

customer → appointments

customer → communications

customer → notes

---

# 94. TESTING — HISTORICAL DATA

Test customer with:

no calls

no leads

no appointments

no communications

old call without transcript

old appointment

archived customer

partial data

UI must not crash.

---

# 95. TESTING — TIMELINE

Test:

events in chronological order

same timestamp events

duplicate events

late events

missing events

pagination

filters

---

# 96. TESTING — ADDRESS

Test:

one address

multiple addresses

changing primary

removing address

historical appointment remains unchanged

---

# 97. TESTING — AI TOOLS

If M16 tools are implemented:

test:

find_customer

create_customer

update_customer

tenant isolation

validation

idempotency

authorization

malicious input

---

# 98. REGRESSION TESTING

After M16:

M10 must continue working.

M11 must continue working.

M11.2 must continue working.

M12 must continue working.

M13 must continue working.

M14 must continue working.

M15 must continue working.

Critical end-to-end:

Customer calls
↓
Customer resolved
↓
Call recorded
↓
Lead created
↓
Appointment booked
↓
SMS sent
↓
Call completed
↓
Customer 360 updated
↓
Timeline updated

---

# 99. DO NOT BUILD THESE IN M16

Do NOT implement:

❌ Full CRM replacement

❌ Sales pipeline

❌ Marketing campaigns

❌ Email marketing

❌ Bulk SMS

❌ Advanced customer segmentation

❌ Customer lifetime value engine

❌ AI customer scoring

❌ Predictive churn

❌ Sentiment engine

❌ AI coaching

❌ Call QA scoring

❌ RAG

❌ Knowledge base

❌ Vector database

❌ Billing

❌ Subscription management

❌ Technician dispatch

❌ Payroll

❌ Inventory

❌ Full accounting

❌ Advanced reporting

❌ Automatic customer merging

❌ Destructive hard-delete

❌ New Twilio credential system

❌ New authentication system

❌ New RBAC system

❌ Duplicate event bus

❌ Duplicate communication system

❌ Duplicate appointment system

Keep M16 focused on Customer 360.

---

# 100. IMPLEMENTATION ORDER

Follow this order.

STEP 1
Inspect M12–M15.

STEP 2
Identify the existing Customer model.

STEP 3
Identify all existing customer references.

STEP 4
Design the canonical Customer entity.

STEP 5
Implement/extend Customer model.

STEP 6
Implement safe customer resolution.

STEP 7
Implement duplicate prevention.

STEP 8
Implement customer addresses.

STEP 9
Implement customer notes.

STEP 10
Implement customer tags.

STEP 11
Implement customer APIs.

STEP 12
Implement customer activity aggregation.

STEP 13
Integrate calls.

STEP 14
Integrate leads.

STEP 15
Integrate appointments.

STEP 16
Integrate communications.

STEP 17
Implement customer metrics.

STEP 18
Implement Customer list UI.

STEP 19
Implement Customer 360 page.

STEP 20
Implement timeline UI.

STEP 21
Implement quick actions.

STEP 22
Integrate AI tools only if required.

STEP 23
Add indexes.

STEP 24
Add security tests.

STEP 25
Add concurrency tests.

STEP 26
Add integration tests.

STEP 27
Run M10–M15 regression.

STEP 28
Fix all errors.

STEP 29
Update documentation.

STEP 30
Provide final implementation report.

---

# 101. CRITICAL ARCHITECTURE RULE

CustomerService should become the central domain service for customer operations.

Do NOT allow:

Controller
→ MongoDB directly

AI Tool
→ MongoDB directly

Frontend
→ MongoDB

Call webhook
→ MongoDB directly

Instead:

Controller
→ CustomerService
→ Repository/Model

AI Tool
→ M11.2
→ CustomerService

Call workflow
→ CustomerService

This prevents business logic duplication.

---

# 102. SOURCE-OF-TRUTH RULE

Customer:

Customer model

Calls:

M15 CallLog

Leads:

M12 Lead

Appointments:

M13 Appointment

Communications:

M14 Communication

Customer notes:

CustomerNote

Do not duplicate entire entities inside Customer.

Use references and derived aggregates.

---

# 103. DATA CONSISTENCY RULE

When customer data changes:

Do not silently rewrite historical records.

Example:

Customer phone changes.

Old call:

must remain historically accurate.

New customer profile:

uses current phone.

If snapshots are needed:

document them explicitly.

---

# 104. EVENTUAL CONSISTENCY

Some Customer 360 data may update asynchronously.

Example:

Appointment completed

↓

event emitted

↓

Customer metrics updated

This is acceptable.

The UI should gracefully refresh/revalidate.

---

# 105. AUDITABILITY

For customer modifications, capture:

who changed it

what changed

when

where appropriate.

Do not create a completely new audit architecture if an existing audit system exists.

Reuse it.

---

# 106. CUSTOMER PROFILE EDITING

Editing must use controlled forms.

Validate:

name
email
phone
addresses
tags
status

Display field-level errors.

Do not silently discard invalid input.

---

# 107. UNSAVED CHANGES

If the existing frontend architecture supports form dirty-state handling:

warn before leaving with unsaved changes.

Do not make forms unnecessarily complex.

---

# 108. CUSTOMER ACTION CONFIRMATION

Destructive/important actions should require confirmation.

Example:

Archive Customer

Modal:

"Archive John Smith?"

"Historical calls, leads, appointments, and communications will remain available."

Do not delete historical data.

---

# 109. API DOCUMENTATION

Document all M16 endpoints.

At minimum:

POST /api/customers

GET /api/customers

GET /api/customers/:id

PATCH /api/customers/:id

POST /api/customers/:id/archive

POST /api/customers/:id/restore

GET /api/customers/:id/activity

GET /api/customers/:id/calls

GET /api/customers/:id/leads

GET /api/customers/:id/appointments

GET /api/customers/:id/communications

POST /api/customers/:id/notes

PATCH /api/customers/:id/notes/:noteId

DELETE /api/customers/:id/notes/:noteId

Only implement endpoints that are actually needed and consistent with existing conventions.

---

# 110. DOCUMENTATION

Update:

README

API documentation

architecture documentation

database documentation

.env.example only if required

Document:

Customer model

relationships

duplicate handling

tenant security

timeline

AI integration

RBAC

indexes

API behavior

limitations

---

# 111. FINAL ACCEPTANCE CHECKLIST

Before declaring M16 complete:

[ ] Existing Customer model inspected
[ ] No duplicate Customer model
[ ] Multi-tenancy enforced
[ ] CustomerService implemented/reused
[ ] Customer CRUD implemented
[ ] Customer archive implemented
[ ] Restore implemented if required
[ ] Phone normalization reused
[ ] Duplicate prevention implemented
[ ] Concurrency protection implemented
[ ] Address management implemented
[ ] Primary address logic implemented
[ ] Historical appointment address preserved
[ ] Customer notes implemented
[ ] Customer tags implemented
[ ] Customer timeline implemented
[ ] Call relationships implemented
[ ] Lead relationships implemented
[ ] Appointment relationships implemented
[ ] Communication relationships implemented
[ ] Customer metrics implemented
[ ] Last contact implemented
[ ] Next appointment implemented
[ ] Customer list implemented
[ ] Customer detail implemented
[ ] Search implemented
[ ] Filters implemented
[ ] Pagination implemented
[ ] Responsive UI implemented
[ ] Loading states implemented
[ ] Empty states implemented
[ ] Error states implemented
[ ] RBAC enforced
[ ] PII-safe logging
[ ] API validation
[ ] NoSQL injection protection
[ ] Proper indexes
[ ] Timeline ordering
[ ] Event deduplication
[ ] AI customer tools controlled
[ ] No chain-of-thought stored
[ ] No credentials exposed
[ ] M10 regression passed
[ ] M11 regression passed
[ ] M11.2 regression passed
[ ] M12 regression passed
[ ] M13 regression passed
[ ] M14 regression passed
[ ] M15 regression passed
[ ] Documentation updated
[ ] No M17+ features prematurely implemented

---

# 112. REQUIRED FINAL REPORT

After implementation, provide:

## M16 STATUS

Complete / Partial / Blocked

## ARCHITECTURE

Explain Customer 360 architecture.

## CUSTOMER MODEL

List fields and indexes.

## RELATIONSHIPS

Explain:

Customer → Calls
Customer → Leads
Customer → Appointments
Customer → Communications
Customer → Notes

## DUPLICATE HANDLING

Explain phone/email matching and concurrency protection.

## TIMELINE

Explain how timeline events are generated.

## APIs

List every implemented endpoint.

## AI INTEGRATION

Explain customer resolution and controlled AI tools.

## SECURITY

Explain:

tenant isolation
RBAC
PII protection
validation

## FRONTEND

List:

Customer List
Customer Detail
Timeline
Notes
Addresses
Quick Actions

## TESTING

List all tests and actual results.

## REGRESSION

Confirm M10–M15 remain functional.

## DATABASE

List models/indexes/relationships.

## ENVIRONMENT

List actual environment changes.

## LIMITATIONS

List anything intentionally deferred.

## NEXT MODULE

State what M17 should build, but DO NOT implement M17.

IMPORTANT:

Never claim a feature is implemented unless it actually exists and works.

Never fabricate test results.

Never fabricate API responses.

Never fabricate database migrations.

Never expose secrets.

Never store chain-of-thought.

END OF M16.

### M16 ke baad tumhare system me important flow

**Customer 360 ab central layer ban jayega:**

`Customer → Calls → Transcripts → Leads → Appointments → SMS → Notes → Complete Timeline`

Example:

**John** pehli baar call karta hai → backend uske phone se customer identify/create karta hai → M15 call record karta hai → M12 lead banata hai → M13 appointment book karta hai → M14 confirmation SMS bhejta hai → M16 Customer 360 me ye sab automatically ek customer profile ke andar connected dikhata hai.

**M17 ko M16 ke baad customer-side relationship ke upar next operational layer banana chahiye; M16 me hi usko prematurely implement mat karna.**
