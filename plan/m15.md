Bilkul. **M14 ke baad M15 ka logical module `Call Logs, Transcripts & Conversation Analytics Engine`** hona chahiye.

Is phase ka main goal hai: **har AI call ka complete, searchable, auditable record banana** — kis customer ne call ki, kya conversation hui, kaunsa tool call hua, lead/appointment kya bana, call ka outcome kya raha, aur dashboard me owner ko kya dikhna chahiye.

# MASTER PROMPT — M15

## CALL LOGS, TRANSCRIPTS & CONVERSATION ANALYTICS ENGINE

You are a senior production SaaS backend + frontend engineer working on an existing multi-tenant AI Employee SaaS platform for US HVAC/home-service businesses.

The product provides an AI receptionist that:

* answers incoming calls through Twilio,
* communicates with customers using an AI voice engine,
* captures customer information,
* creates/updates leads,
* checks availability,
* books/reschedules/cancels appointments,
* sends SMS through the existing communication engine,
* and records operational events.

You are now implementing:

# M15 — CALL LOGS, TRANSCRIPTS & CONVERSATION ANALYTICS ENGINE

This module must integrate with the existing M0–M14 architecture.

DO NOT rebuild existing systems unnecessarily.

---

# 1. PRIMARY OBJECTIVE

Build a production-ready call history and conversation intelligence layer that allows a business user to answer:

1. Who called?
2. When did they call?
3. Was the call answered?
4. How long did it last?
5. What was the customer asking for?
6. What did the AI do?
7. Which tools were executed?
8. Was a lead created?
9. Was an appointment booked?
10. Was an SMS sent?
11. What was the final call outcome?
12. What should the business do next?
13. Can the business review the transcript?
14. Can the business search previous conversations?
15. Can the business trace the complete customer → lead → appointment → call → communication timeline?

The system must provide reliable operational records rather than speculative AI judgments.

---

# 2. IMPORTANT EXISTING ARCHITECTURE

Before writing code:

INSPECT THE EXISTING PROJECT.

Inspect:

* M10 AI Voice Engine
* M11 AI Tool Calling Engine
* M11.2 Tool Execution Reliability & Control Layer
* M12 Lead Automation Engine
* M13 Appointment & Scheduling Engine
* M14 Communication & SMS Automation Engine
* existing auth
* RBAC
* tenant/business middleware
* event system
* logger
* database utilities
* MongoDB/Mongoose models
* Twilio integration
* WebSocket/realtime voice implementation
* idempotency implementation
* error handling
* API response conventions
* frontend design system
* dashboard layout
* existing CallLog/VoiceSession models if any

DO NOT create duplicate:

* CallLog models
* VoiceSession models
* Twilio clients
* authentication middleware
* tenant middleware
* event infrastructure
* tool executor
* communication service
* logging system
* idempotency system

If an existing implementation is incomplete, EXTEND it safely.

Do not blindly overwrite existing code.

---

# 3. MULTI-TENANT SECURITY

This is a strict SaaS requirement.

Every call-related record must contain:

businessId

Tenant ownership must be enforced server-side.

NEVER trust:

* businessId from frontend
* businessId from AI
* businessId from request body
* businessId from query parameters as an authorization mechanism

Derive business identity from authenticated context.

Example:

Authenticated User
→ Business Membership
→ businessId
→ CallLog query

Every query must enforce:

businessId = authenticatedBusinessId

A user from Business A must NEVER be able to:

* view Business B calls
* search Business B transcripts
* access Business B customers
* access Business B leads
* access Business B appointments
* access Business B communication records

Test cross-tenant access explicitly.

---

# 4. CALL DOMAIN MODEL

Create or safely extend:

CallLog

Suggested fields:

* businessId
* customerId
* leadId
* appointmentId
* callSid
* sessionId
* direction
* phoneFrom
* phoneTo
* normalizedFrom
* normalizedTo
* startedAt
* answeredAt
* endedAt
* durationSeconds
* status
* outcome
* disconnectReason
* transferStatus
* voicemailStatus
* aiProvider
* aiModel
* voiceProvider
* metadata
* summary
* issue
* nextAction
* createdAt
* updatedAt

Do not expose sensitive provider credentials.

---

# 5. CALL DIRECTION

Use controlled enum values:

INBOUND
OUTBOUND

Do not allow arbitrary strings.

---

# 6. CALL STATUS

Use controlled lifecycle values such as:

INITIATED
RINGING
ANSWERED
IN_PROGRESS
COMPLETED
MISSED
FAILED
BUSY
NO_ANSWER
CANCELLED
TRANSFERRED

Only use statuses that make sense with the existing Twilio/voice architecture.

Document valid state transitions.

Example:

INITIATED
→ RINGING
→ ANSWERED
→ IN_PROGRESS
→ COMPLETED

Missed call:

INITIATED
→ RINGING
→ NO_ANSWER

Failure:

INITIATED
→ FAILED

Do not allow arbitrary invalid transitions.

---

# 7. CALL OUTCOME TAXONOMY

Create a controlled outcome taxonomy.

Example:

LEAD_CREATED
LEAD_QUALIFIED
APPOINTMENT_REQUESTED
APPOINTMENT_BOOKED
APPOINTMENT_RESCHEDULED
APPOINTMENT_CANCELLED
INFORMATION_PROVIDED
CALLBACK_REQUESTED
SMS_REQUESTED
TRANSFERRED
MISSED_CALL
NO_ACTION
CUSTOMER_DECLINED
WRONG_NUMBER
SPAM
TECHNICAL_FAILURE
OTHER

Do not automatically assign an outcome merely because a call ended.

Outcome should be based on actual recorded events and structured signals.

---

# 8. CALL IDENTIFIERS

Support multiple identifiers:

callSid
sessionId
businessId

Twilio's call SID should be stored when available.

Use provider identifiers for correlation but do not expose unnecessary provider internals to customers.

Call SID should have an appropriate unique/index strategy.

If duplicate webhooks arrive:

DO NOT create duplicate calls.

Use idempotent processing.

---

# 9. CALL LIFECYCLE

Implement reliable call lifecycle tracking.

Typical flow:

Twilio call
↓
Voice session created
↓
CallLog created
↓
Call answered
↓
AI conversation starts
↓
Conversation turns recorded
↓
Tool calls recorded
↓
Lead/appointment/communication events linked
↓
Call ends
↓
Final outcome calculated
↓
Summary generated
↓
CallLog finalized

The exact implementation must follow the existing M10 architecture.

---

# 10. TRANSCRIPT MODEL

Create a transcript representation associated with a CallLog.

Possible model:

CallTranscript

Fields:

* businessId
* callId
* sessionId
* version
* language
* turns
* status
* startedAt
* completedAt
* createdAt
* updatedAt

Transcript turns should contain:

* turnId
* speaker
* timestamp
* sequence
* text
* duration
* toolCallId
* metadata

Speaker enum:

CUSTOMER
AI
SYSTEM

Do not expose hidden reasoning.

---

# 11. TRANSCRIPT STORAGE RULE

Store the actual conversational content required for business operations.

DO NOT store:

* chain-of-thought
* hidden reasoning
* internal model deliberation
* secret prompts
* API keys
* provider credentials
* internal security instructions

If a tool was executed, record the structured tool event/reference, not hidden reasoning.

Example:

AI:
"Would you like me to check tomorrow afternoon?"

Tool:
check_availability

Result:
2 available slots

This is acceptable.

Do not store:

"AI internally reasoned for 4 steps..."

---

# 12. TRANSCRIPT TURN EXAMPLE

A transcript should conceptually look like:

CUSTOMER
"Hi, my AC is blowing warm air."

AI
"I can help with that. What is the service address?"

CUSTOMER
"123 Main Street."

AI
"Would you like me to check the next available appointment?"

Tool Event
check_availability

Tool Result
2026-09-18 10:00 AM
2026-09-18 2:00 PM

AI
"I have 10 AM or 2 PM available tomorrow."

CUSTOMER
"2 PM."

Tool Event
create_appointment

Tool Result
confirmed

AI
"You're booked for tomorrow at 2 PM."

This should be reconstructable from stored data.

---

# 13. TRANSCRIPT ORDERING

Never rely only on MongoDB insertion order.

Each transcript turn should have:

sequence

Example:

1 CUSTOMER
2 AI
3 CUSTOMER
4 AI
5 TOOL
6 AI

Use sequence for deterministic reconstruction.

Timestamp should also be stored where available.

---

# 14. TRANSCRIPT SEARCH

Implement backend support for searching transcripts.

Possible filters:

* businessId
* date range
* customer
* phone number
* lead
* appointment
* call outcome
* call status
* direction
* transcript text

Search must remain tenant scoped.

For MongoDB, use an appropriate indexing/search strategy based on the existing project.

Do not introduce Elasticsearch/OpenSearch unless already required by the architecture.

Start with MongoDB-native capabilities.

---

# 15. CUSTOMER PHONE SEARCH

Normalize phone numbers consistently.

If the project already has phone normalization:

REUSE IT.

Do not create a second phone normalization implementation.

Never expose unnecessary PII in logs.

For UI:

Display phone numbers according to the existing privacy conventions.

---

# 16. CALL SUMMARY ENGINE

Each completed call should optionally have a structured summary.

Example:

Summary:
"Customer reported AC blowing warm air and requested a service appointment."

Issue:
"AC blowing warm air."

Outcome:
"Appointment booked."

Next action:
"Technician visit scheduled for September 18 at 2 PM."

Do not generate speculative information.

Do not claim:

"Customer was definitely angry."

unless the system has an explicit supported signal.

---

# 17. SUMMARY STRUCTURE

Use structured fields:

summary
issue
requestedService
outcome
nextAction

Optionally:

customerIntent
serviceAddressCaptured
appointmentBooked
leadCreated

These should be derived from actual structured events wherever possible.

---

# 18. AI SUMMARY PROVIDER

If summary generation requires an LLM:

Reuse the existing AI provider abstraction.

Existing routing preference:

Primary:
Azure OpenAI

Secondary:
OpenAI

Third:
Google Gemini

Provider/model selection must remain configuration-driven.

Do NOT expose API keys to:

* frontend
* browser
* AI voice client
* customer

Do not hardcode provider credentials.

---

# 19. AI SUMMARY SAFETY

Summary generation must be:

* deterministic where possible
* concise
* factual
* based on transcript + structured events
* free from chain-of-thought
* free from invented facts

If summary generation fails:

The call must STILL be stored successfully.

Do not fail the entire call record because summary generation failed.

Use fallback:

summary = null

and mark summary generation status appropriately.

---

# 20. TOOL CALL LINKING

Integrate with M11/M11.2.

For each tool execution during a call, record a safe structured reference.

Example:

toolName:
check_availability

status:
SUCCEEDED

startedAt
completedAt

toolCallId

Do not duplicate the entire tool execution engine.

Use existing tool execution records if available.

Call analytics should be able to show:

Tool Calls:

* create_lead — succeeded
* check_availability — succeeded
* create_appointment — succeeded
* send_sms — succeeded

---

# 21. TOOL CALL METRICS

Call-level analytics may include:

toolCallCount
successfulToolCalls
failedToolCalls
toolFailureCount

These are factual operational metrics.

Do not turn these numbers into unsupported claims about AI quality.

---

# 22. LEAD LINKING

A call can be associated with:

customerId
leadId

When M12 creates a lead from a call:

Update the call linkage.

Example:

Call
→ Customer
→ Lead

Do not create duplicate leads simply because the same call is processed more than once.

Use the existing M12 idempotency logic.

---

# 23. APPOINTMENT LINKING

A call may create or modify an appointment.

Maintain:

appointmentId

When an appointment is booked during the call:

Call
→ Customer
→ Lead
→ Appointment

The call detail page should make this relationship visible.

Do not duplicate appointment data inside CallLog beyond useful snapshot fields.

The Appointment model remains the source of truth.

---

# 24. COMMUNICATION LINKING

Integrate with M14.

If an SMS is sent during or after a call:

Call
→ Communication

Do not duplicate communication records.

Use communicationId/reference where available.

Example:

Call completed
↓
Appointment booked
↓
Confirmation SMS
↓
Communication record
↓
Call timeline displays SMS event

---

# 25. CALL TIMELINE

Create a unified call timeline.

Example:

10:31:02 — Call started
10:31:05 — Customer connected
10:31:12 — Customer reported AC issue
10:32:10 — Lead created
10:33:20 — Availability checked
10:34:02 — Appointment booked
10:34:08 — Confirmation SMS queued
10:35:30 — Call completed

The timeline should combine relevant events from:

* call lifecycle
* transcript
* tool execution
* lead events
* appointment events
* communication events

Do not duplicate event systems unnecessarily.

Prefer existing domain events.

---

# 26. MISSED CALL HANDLING

M14 depends on missed-call events.

M15 should record missed calls correctly.

Example:

Incoming call
↓
No answer
↓
CallLog.status = NO_ANSWER
↓
CallLog.outcome = MISSED_CALL
↓
M14 can consume missed-call event
↓
Missed-call SMS may be sent

Do not send SMS directly from M15.

M15 records/emits the call event.

M14 owns communication execution.

---

# 27. TWILIO WEBHOOK INTEGRATION

Inspect existing Twilio webhook infrastructure.

Do NOT create duplicate webhook architecture.

If call lifecycle webhooks already exist:

extend them.

If not:

implement the minimum required call webhook handling.

Validate Twilio signatures where applicable.

Handle:

* duplicate webhook delivery
* out-of-order events
* malformed payloads
* unknown call SID
* already completed call
* retry delivery

Webhook processing must be idempotent.

---

# 28. OUT-OF-ORDER EVENTS

Do not assume webhook events always arrive perfectly ordered.

Example:

COMPLETED arrives before another metadata update.

The system should merge valid information without corrupting the call lifecycle.

Never move a call backward into an invalid state merely because an older webhook arrived late.

---

# 29. CALL DURATION

Calculate duration from actual timestamps.

Preferred:

endedAt - answeredAt

If unanswered:

duration may be based on the available ring/call lifecycle information according to the existing architecture.

Never invent duration.

Store seconds as numeric value.

---

# 30. CONVERSATION METRICS

Support factual metrics such as:

* duration
* transcript turn count
* customer turn count
* AI turn count
* tool call count
* successful tool calls
* failed tool calls
* appointment booked
* lead created
* SMS sent
* transfer occurred
* interruption count if reliably available

Only calculate metrics when source data exists.

If data is unavailable:

return null or omit it.

Do not fabricate.

---

# 31. TALK/LISTEN METRICS

If M10 provides reliable speech timing:

support:

customerTalkSeconds
aiTalkSeconds

and optionally:

customerTalkRatio
aiTalkRatio

If the voice engine does not provide reliable timing:

DO NOT estimate these values.

Return unavailable rather than fake analytics.

---

# 32. INTERRUPTION METRICS

If the realtime voice engine provides actual interruption/barge-in events:

store:

interruptionCount

Otherwise:

do not invent it.

---

# 33. TRANSFER TRACKING

Support:

transferRequested
transferStarted
transferCompleted
transferFailed

if the existing voice system provides these signals.

The UI should distinguish:

AI call
→ transferred call

from:

AI call
→ completed normally.

---

# 34. RECORDING

DO NOT automatically introduce raw audio recording storage unless the existing architecture already requires it.

If call recordings already exist:

integrate metadata only.

Possible fields:

recordingAvailable
recordingId
recordingDuration
recordingStatus

If recordings are introduced later, they must have:

* proper authorization
* retention policy
* secure access
* signed/private URLs
* tenant isolation
* applicable consent/legal controls

Do not expose public recording URLs.

Do not build a large recording-storage subsystem in M15 unless explicitly required.

---

# 35. CALL DETAIL API

Implement an endpoint such as:

GET /api/calls/:id

Response should provide:

* call metadata
* customer reference
* lead reference
* appointment reference
* call status
* call outcome
* duration
* summary
* issue
* next action
* transcript
* tool events
* linked communications
* timeline
* metrics

Everything must be tenant scoped.

---

# 36. CALL LIST API

Implement:

GET /api/calls

Support:

page
limit
search
status
outcome
direction
customerId
leadId
appointmentId
startDate
endDate

Example:

GET /api/calls?page=1&limit=25&status=COMPLETED

Validate every query parameter.

Never allow arbitrary MongoDB operators through query parameters.

---

# 37. CALL SEARCH

Search can support:

customer name
phone number
call SID where appropriate
transcript text
appointment number
lead identifier

Search results should be paginated.

Do not return the complete transcript for every row in the list API.

Use summary fields in list responses.

Load transcript only on the call detail page.

---

# 38. PAGINATION

Implement stable pagination.

Preferred approach depends on the existing project.

Offset pagination is acceptable initially:

page
limit

But enforce:

maximum limit

Example:

default = 25
maximum = 100

Never allow:

limit=1000000

---

# 39. CALL DASHBOARD

Create:

/dashboard/calls

The page should include:

## Header

Calls

Description:
"Review customer calls, conversations, outcomes, and follow-up activity."

## Summary cards

Total Calls
Answered
Missed
Appointments Booked
Leads Created

Use real API data.

Do not hardcode numbers.

---

# 40. CALL LIST UI

Each row should display:

Customer
Phone
Direction
Date/Time
Duration
Outcome
Lead
Appointment
Status

Example:

John Smith
+1 (555) ***-1234
Inbound
Sep 18, 10:31 AM
04:28
Appointment Booked

---

# 41. FILTERS

Provide:

Date range
Direction
Status
Outcome
Customer
Lead
Appointment

Filters should update the backend query.

Do not fetch every call and filter only in the browser.

---

# 42. CALL DETAIL PAGE

Create:

/dashboard/calls/[id]

Recommended layout:

LEFT / MAIN:

Call information
Summary
Issue
Outcome
Next Action
Transcript
Timeline

RIGHT:

Customer
Lead
Appointment
Communication
Call metrics

The layout must remain responsive.

---

# 43. TRANSCRIPT UI

Build a professional transcript viewer.

Example:

CUSTOMER
"Hi, my AC is blowing warm air."

AI
"I can help with that. What's the service address?"

CUSTOMER
"123 Main Street."

Tool
check_availability
✓ Successful

AI
"I have 10 AM and 2 PM available."

Use visually distinct speaker types.

Do not make the UI look like a basic chat toy.

---

# 44. TRANSCRIPT FEATURES

Support:

* chronological display
* speaker labels
* timestamps
* tool event indicators
* transcript search
* auto-scroll where appropriate
* loading state
* empty state
* error state

If transcript is long:

use pagination/chunking/virtualization where appropriate.

Do not render thousands of turns inefficiently.

---

# 45. TIMELINE UI

Display events in chronological order.

Example:

CALL STARTED
10:31 AM

LEAD CREATED
10:32 AM

AVAILABILITY CHECKED
10:33 AM

APPOINTMENT BOOKED
10:34 AM

SMS SENT
10:34 AM

CALL COMPLETED
10:35 AM

Clicking an event may navigate to the associated resource.

---

# 46. CUSTOMER LINK

On the call detail page:

Customer card

Show:

Name
Phone
Email if available
Address if authorized/available

Provide:

View Customer

Do not expose information the user is not authorized to access.

---

# 47. LEAD LINK

If lead exists:

Show:

Lead status
Service type
Urgency
Qualification
Created date

Button:

View Lead

Reuse M12 APIs/UI conventions.

---

# 48. APPOINTMENT LINK

If appointment exists:

Show:

Appointment number
Date
Time
Status
Service type

Button:

View Appointment

Reuse M13 source-of-truth data.

---

# 49. COMMUNICATION LINK

If SMS was sent:

Show:

SMS
Status
Timestamp
Type

Button:

View Communication

Reuse M14 data.

---

# 50. CALL OUTCOME LOGIC

Build a deterministic outcome resolution strategy.

Priority should generally come from confirmed domain events.

Example:

Appointment successfully booked
→ APPOINTMENT_BOOKED

Lead qualified but no appointment
→ LEAD_QUALIFIED

Lead created but not qualified
→ LEAD_CREATED

No answer
→ MISSED_CALL

Transfer completed
→ TRANSFERRED

Otherwise:
→ INFORMATION_PROVIDED / NO_ACTION depending on actual structured signals

Document the exact precedence.

Do not let an LLM arbitrarily overwrite confirmed system outcomes.

---

# 51. CALL SUMMARY GENERATION TIMING

Do not block call completion on LLM summarization.

Preferred:

Call ends
↓
CallLog finalized
↓
Event emitted
↓
Summary job runs
↓
Summary stored

If summary generation fails:

Call remains completed.

Retry according to the existing job/retry infrastructure.

---

# 52. EVENT INTEGRATION

Consume or react to existing events where appropriate:

call.started
call.answered
call.completed
call.missed
call.failed
lead.created
lead.qualified
appointment.created
appointment.confirmed
communication.sent
communication.delivered

Do not duplicate events that already exist.

If an event schema already exists:

reuse it.

---

# 53. DOMAIN EVENTS

If call events are not already standardized, define:

call.created
call.started
call.answered
call.completed
call.missed
call.failed
call.transcript_updated
call.summary_generated
call.outcome_updated

Include:

eventId
businessId
callId
timestamp
source
correlationId

Use the existing event architecture if present.

---

# 54. IDEMPOTENCY

Use existing M11.2 reliability infrastructure.

Important idempotency cases:

* duplicate Twilio webhook
* duplicate call creation
* duplicate transcript turn
* duplicate call completion
* duplicate summary job
* duplicate event consumption

A single call must never become multiple CallLog records because the provider retried a webhook.

---

# 55. CORRELATION IDs

Support tracing:

businessId
callId
sessionId
callSid
leadId
appointmentId
communicationId
eventId
correlationId

Logs should make it possible to trace:

Twilio Call
→ AI Session
→ Tool Execution
→ Lead
→ Appointment
→ SMS

---

# 56. LOGGING

Use existing logger.

Never log:

* API keys
* Twilio auth tokens
* Azure keys
* OpenAI keys
* Gemini keys
* complete sensitive customer data unnecessarily
* hidden system prompts
* chain-of-thought

Prefer:

callId
businessId
sessionId
callSid
eventId

---

# 57. RBAC

Respect existing roles.

Typical access:

OWNER
ADMIN
MANAGER
STAFF

Follow the project's existing authorization model.

Do not invent a completely separate permissions system.

If sensitive transcript access requires a permission:

reuse/extend existing RBAC.

---

# 58. TRANSCRIPT PRIVACY

Transcript data may contain:

* phone numbers
* addresses
* names
* emails
* service information

Therefore:

* protect API access
* tenant-scope all queries
* avoid public URLs
* avoid exposing transcripts in logs
* apply existing data-retention rules
* use secure transport

Do not build a separate privacy architecture if one already exists.

---

# 59. RETENTION-READY DESIGN

Add architecture support for future retention policies.

Possible:

retentionExpiresAt

Do not implement destructive automatic deletion unless the existing product policy explicitly requires it.

M15 should be retention-policy ready.

---

# 60. ANALYTICS API

Create a call analytics endpoint such as:

GET /api/calls/analytics

Possible response:

{
totalCalls,
answeredCalls,
missedCalls,
completedCalls,
averageDurationSeconds,
appointmentsBooked,
leadsCreated,
transfers,
toolCalls,
toolFailures
}

All values must come from actual database/event data.

---

# 61. DATE RANGE ANALYTICS

Support:

today
yesterday
last7Days
last30Days
custom

Do not hardcode timezone assumptions.

Use the business's configured timezone where appropriate.

Database timestamps should remain consistently stored.

---

# 62. TIMEZONE

Calls should be stored using UTC timestamps.

Business timezone should control display.

Example:

Database:
2026-09-18T06:31:00Z

Business timezone:
America/New_York

UI displays the correct local business time.

Do not calculate timezone from phone area code.

---

# 63. ANALYTICS ACCURACY

If a metric cannot be reliably calculated:

return:

null

or:

unavailable

Do not produce fake analytics.

Example:

If speech timing is unavailable:

averageCustomerTalkRatio = null

---

# 64. NO SPECULATIVE AI QUALITY SCORING

Do NOT implement:

* "AI intelligence score"
* "AI competence score"
* "AI IQ"
* "AI was 87% good"
* unsupported emotional judgments
* unsupported customer sentiment claims

M15 is an operational analytics system.

Advanced QA/coaching can be a future module.

---

# 65. SENTIMENT

Do not make sentiment analysis a core requirement.

If the existing architecture already provides a sentiment signal:

store it explicitly as:

heuristic/AI-derived

and document that it is not a factual measurement.

Otherwise leave sentiment for a future module.

---

# 66. PERFORMANCE

Call list APIs must remain fast.

Create appropriate indexes.

Potential indexes:

{
businessId: 1,
createdAt: -1
}

{
businessId: 1,
status: 1,
createdAt: -1
}

{
businessId: 1,
outcome: 1,
createdAt: -1
}

{
businessId: 1,
customerId: 1,
createdAt: -1
}

{
businessId: 1,
leadId: 1
}

{
businessId: 1,
appointmentId: 1
}

Unique/lookup index for provider call identity should include tenant safety as appropriate.

Do not create unnecessary indexes blindly.

Inspect actual query patterns first.

---

# 67. TRANSCRIPT INDEXING

If transcript search is implemented through MongoDB text indexes or another existing search abstraction:

make it tenant-safe.

Do not expose arbitrary database query operators.

Do not introduce a heavyweight search engine just for M15.

---

# 68. API VALIDATION

Every endpoint must validate:

* ObjectId
* dates
* pagination
* enums
* search strings
* IDs
* date ranges

Reject malformed input with the existing error format.

---

# 69. ERROR HANDLING

Use existing centralized error handling.

Examples:

Call not found
→ 404

Unauthorized call
→ 403/404 according to existing security convention

Invalid ID
→ 400

Invalid filter
→ 400

Transcript unavailable
→ graceful response

Summary unavailable
→ call remains accessible

Never leak stack traces to the frontend.

---

# 70. FRONTEND STATES

Every M15 page must support:

Loading
Empty
Error
Success
Partial data

Example empty state:

"No calls yet"

Example transcript empty:

"No transcript is available for this call."

Do not display broken blank screens.

---

# 71. RESPONSIVE UI

Desktop:

Sidebar
→ Calls page
→ Call table
→ Detail workspace

Mobile:

Bottom navigation / responsive navigation
→ Calls
→ Call detail
→ Transcript

The UI must be usable on:

* desktop
* tablet
* mobile

---

# 72. UI QUALITY

DO NOT create childish/basic UI.

Use the existing design system.

Maintain:

* professional SaaS appearance
* consistent typography
* spacing
* cards
* tables
* badges
* drawers/modals
* responsive layouts
* accessible buttons
* skeleton loading
* empty states
* error states

Reuse existing components wherever possible.

Do not create duplicate button/card/table systems.

---

# 73. CALL SEARCH UX

Provide a prominent search field:

"Search calls, customers, phone numbers, or transcripts..."

Debounce search where appropriate.

Do not issue an API request on every keystroke without protection.

---

# 74. CALL TABLE ACTIONS

Each row can have:

View
Customer
Lead
Appointment

Do not add destructive call deletion by default.

Call records are operational/audit history.

---

# 75. SOFT DELETE

Do not hard-delete calls as a normal UI action.

If administrative deletion is ever required:

it must follow the product's retention/audit policy.

M15 should preserve operational history.

---

# 76. CALL DETAIL URL

Use:

/dashboard/calls/:id

or the existing Next.js routing convention.

Ensure unauthorized IDs cannot reveal cross-tenant records.

---

# 77. API RESPONSE CONSISTENCY

Follow existing project response conventions.

Do not introduce a completely new response shape.

If existing pattern is:

{
success: true,
data: ...
}

continue using it.

---

# 78. FRONTEND DATA FETCHING

Reuse existing API client.

Do not create multiple Axios/fetch wrappers.

Reuse:

* auth handling
* interceptors
* error handling
* token/session behavior

---

# 79. CUSTOMER TIMELINE PREPARATION

Design M15 so that future customer 360 view can show:

Customer
→ Calls
→ Leads
→ Appointments
→ SMS
→ Follow-ups

Do not build the complete Customer 360 module now.

Only expose clean relationships.

---

# 80. FUTURE EXTENSIBILITY

Architecture should be ready for future:

* call quality assurance
* AI coaching
* agent performance
* advanced conversation analytics
* sentiment
* keyword analytics
* recording management
* supervisor review
* call tagging
* compliance review

BUT DO NOT IMPLEMENT THESE FULL SYSTEMS IN M15.

---

# 81. WHAT M15 MUST NOT BUILD

Do NOT build:

❌ Advanced AI coaching
❌ Agent scoring system
❌ AI quality score
❌ RAG
❌ Knowledge base
❌ Vector database
❌ Marketing automation
❌ Email campaigns
❌ Billing
❌ Subscription management
❌ Technician dispatch
❌ Payroll
❌ Full CRM
❌ Full customer 360
❌ Google Calendar synchronization
❌ Outlook synchronization
❌ Advanced call recording storage
❌ Bulk SMS
❌ Marketing campaigns
❌ New Twilio credential architecture
❌ New authentication system
❌ New RBAC system
❌ New tenant system
❌ New tool executor
❌ New event bus if one already exists

Keep M15 focused.

---

# 82. TESTING REQUIREMENTS

Create comprehensive tests.

## Call lifecycle

Test:

INITIATED
RINGING
ANSWERED
IN_PROGRESS
COMPLETED

## Missed calls

Test:

NO_ANSWER
MISSED_CALL

## Failed calls

Test provider failure.

## Duplicate webhook

Same webhook twice must not duplicate the call.

## Out-of-order webhook

Old event must not corrupt final state.

## Transcript

Test:

* turn creation
* sequence
* ordering
* duplicate turn protection
* speaker validation

## Tenant isolation

Business A cannot access Business B calls.

## Search

Test:

customer
phone
transcript
status
outcome
date range

## Pagination

Test:

default limit
maximum limit
page boundaries

## Linking

Test:

call → customer
call → lead
call → appointment
call → communication

## Summary

Test:

successful summary
failed summary
retry
call remains available if summary fails

## Analytics

Test:

counts
duration
appointments
leads
tool calls

---

# 83. SECURITY TESTING

Explicitly test:

* cross-tenant CallLog access
* cross-tenant transcript access
* unauthorized call detail
* unauthorized transcript search
* malicious query parameters
* NoSQL injection attempts
* invalid ObjectIds
* oversized search queries
* pagination abuse

---

# 84. REGRESSION TESTING

After implementation:

Run/verify:

M10 voice engine
M11 tool calling
M11.2 reliability
M12 leads
M13 appointments
M14 communications

Critical flows must continue working:

Incoming call
→ AI answers
→ Lead created
→ Availability checked
→ Appointment booked
→ Confirmation SMS
→ Call completed
→ CallLog finalized
→ Transcript stored
→ Summary generated
→ Dashboard displays complete timeline

---

# 85. END-TO-END TEST

Create at least one realistic end-to-end scenario.

Example:

Customer calls HVAC business.

Customer:
"My AC isn't cooling."

AI collects:

* name
* phone
* address
* service issue

AI creates lead.

AI checks availability.

Customer selects slot.

AI books appointment.

M14 sends confirmation SMS.

Call ends.

M15 records:

CallLog
Transcript
Tool events
Lead link
Appointment link
Communication link
Summary
Outcome

Dashboard then displays the complete interaction.

---

# 86. DATA CONSISTENCY

The following must remain separate sources of truth:

Customer
→ Customer model

Lead
→ Lead model

Appointment
→ Appointment model

Communication
→ Communication model

Call
→ CallLog

Transcript
→ CallTranscript

Do not duplicate entire objects unnecessarily.

Use IDs/references.

---

# 87. AUDITABILITY

A business admin should be able to reconstruct:

What happened?
When did it happen?
Which call caused it?
Which tool was executed?
What entity was created?
What communication was sent?

The system should provide enough structured data to answer those questions.

---

# 88. DOCUMENTATION

Update:

README
API documentation
environment documentation if changed
architecture documentation

Document:

CallLog schema
Transcript schema
Call lifecycle
Outcome taxonomy
API endpoints
Webhook handling
Indexes
Tenant security
Summary generation
Analytics
Known limitations

---

# 89. ENVIRONMENT VARIABLES

Do not create unnecessary new environment variables.

If summary generation requires provider configuration:

reuse the existing AI provider environment architecture.

If new values are genuinely required:

update:

.env.example

and documentation.

NEVER commit secrets.

---

# 90. DATABASE MIGRATION

If MongoDB/Mongoose schema changes are required:

implement them safely.

Existing data must not become unreadable.

If existing CallLog records lack new fields:

support backward compatibility.

Do not assume every historical call has a transcript.

---

# 91. BACKWARD COMPATIBILITY

Historical records may have:

* no leadId
* no appointmentId
* no transcript
* no summary
* no outcome
* incomplete duration

The UI must handle these safely.

Example:

"No transcript available"

rather than crashing.

---

# 92. FINAL M15 USER EXPERIENCE

The business owner should be able to:

1. Open Calls.
2. See all calls.
3. Filter calls.
4. Search calls.
5. Open a call.
6. Read summary.
7. Read transcript.
8. See tool executions.
9. See lead information.
10. See appointment information.
11. See SMS communication.
12. See call timeline.
13. Understand final outcome.
14. Review basic factual analytics.

The experience should feel like a real professional SaaS product.

---

# 93. IMPLEMENTATION ORDER

Implement in this order:

STEP 1
Inspect M10–M14.

STEP 2
Identify existing CallLog/VoiceSession infrastructure.

STEP 3
Extend/create CallLog model.

STEP 4
Implement transcript model.

STEP 5
Implement call lifecycle handling.

STEP 6
Implement webhook idempotency.

STEP 7
Implement transcript persistence.

STEP 8
Implement tool-event linkage.

STEP 9
Implement lead/appointment/communication linkage.

STEP 10
Implement summary generation.

STEP 11
Implement call outcome resolution.

STEP 12
Implement call APIs.

STEP 13
Implement analytics APIs.

STEP 14
Implement Calls dashboard.

STEP 15
Implement Call Detail page.

STEP 16
Implement transcript viewer.

STEP 17
Implement timeline.

STEP 18
Implement filters/search/pagination.

STEP 19
Add tests.

STEP 20
Run full regression.

STEP 21
Fix all errors.

STEP 22
Document the implementation.

---

# 94. IMPORTANT CODING RULE

Before creating any file:

SEARCH THE PROJECT.

Before creating any model:

SEARCH FOR EXISTING MODEL.

Before creating any service:

SEARCH FOR EXISTING SERVICE.

Before creating any middleware:

SEARCH FOR EXISTING MIDDLEWARE.

Before creating any Twilio integration:

SEARCH FOR EXISTING TWILIO INTEGRATION.

Before creating any event:

SEARCH FOR EXISTING EVENT.

Before creating any API client:

SEARCH FOR EXISTING API CLIENT.

Avoid duplicate architecture.

---

# 95. PRODUCTION QUALITY REQUIREMENT

Do not stop at:

"schema created."

The feature is complete only when:

Backend
+
Database
+
Events
+
Twilio integration
+
Transcript
+
Summary
+
Analytics
+
APIs
+
Frontend
+
Security
+
Tenant isolation
+
Tests
+
Documentation

are working together.

---

# 96. FINAL ACCEPTANCE CHECKLIST

Before declaring M15 complete, verify:

[ ] Existing architecture inspected
[ ] No duplicate systems created
[ ] CallLog implemented
[ ] Call lifecycle implemented
[ ] Twilio call identifiers handled
[ ] Webhooks idempotent
[ ] Out-of-order events handled
[ ] Transcript implemented
[ ] Transcript turns ordered
[ ] Chain-of-thought NOT stored
[ ] Tool calls linked
[ ] Lead linked
[ ] Appointment linked
[ ] Communication linked
[ ] Call outcome implemented
[ ] Call summary implemented
[ ] Summary failure does not break calls
[ ] Call analytics implemented
[ ] Tenant isolation enforced
[ ] RBAC enforced
[ ] PII-safe logging implemented
[ ] Call search implemented
[ ] Pagination implemented
[ ] Date filtering implemented
[ ] Calls dashboard implemented
[ ] Call detail page implemented
[ ] Transcript UI implemented
[ ] Timeline implemented
[ ] Loading states implemented
[ ] Empty states implemented
[ ] Error states implemented
[ ] Responsive UI implemented
[ ] API validation implemented
[ ] MongoDB indexes reviewed
[ ] Security tests implemented
[ ] Regression tests passed
[ ] M10 regression passed
[ ] M11 regression passed
[ ] M11.2 regression passed
[ ] M12 regression passed
[ ] M13 regression passed
[ ] M14 regression passed
[ ] Documentation updated
[ ] .env.example updated only if required
[ ] No secrets committed
[ ] No M16+ features implemented prematurely

---

# 97. REQUIRED FINAL REPORT

When implementation is complete, provide a final report with:

## 1. M15 Status

Complete / Partial / Blocked

## 2. Architecture

Explain how M15 integrates with M10–M14.

## 3. Files Created

List all newly created files.

## 4. Files Modified

List all modified files.

## 5. Database

List models, fields, indexes and relationships.

## 6. Call Lifecycle

Explain all supported states.

## 7. Transcript

Explain transcript storage and retrieval.

## 8. Tool Integration

Explain how M11/M11.2 events are linked.

## 9. Lead Integration

Explain M12 linkage.

## 10. Appointment Integration

Explain M13 linkage.

## 11. Communication Integration

Explain M14 linkage.

## 12. Twilio

Explain how call webhooks and provider identifiers are handled.

## 13. Summary

Explain provider routing and fallback behavior.

## 14. Analytics

List all available metrics.

## 15. APIs

List every M15 endpoint.

## 16. Frontend

List all M15 pages/components.

## 17. Security

Explain tenant isolation, RBAC and PII handling.

## 18. Idempotency

Explain duplicate webhook/event protection.

## 19. Tests

List tests and their result.

## 20. Regression

Confirm M10–M14 continue working.

## 21. Environment

List any new environment variables.

## 22. Limitations

Clearly identify anything intentionally deferred.

## 23. Next Module

State what M16 should build WITHOUT implementing it.

IMPORTANT:

Do not claim something is implemented if it is not actually implemented.

Do not hide errors.

Do not fabricate test results.

Do not say "production ready" unless the implementation and tests actually support that claim.

END OF M15.

### M15 ke baad architecture roughly aisa hoga

**Twilio → M10 Voice Engine → M11/M11.2 Tool System → M12 Lead → M13 Appointment → M14 SMS → M15 Call History/Transcript/Analytics**

Example:

> Customer calls HVAC company → Twilio call receive karta hai → Azure OpenAI AI receptionist customer se baat karta hai → M11 tool se lead create hota hai → M13 availability check karke appointment book hota hai → M14 confirmation SMS bhejta hai → M15 poori call, transcript, tool actions, lead, appointment, SMS aur final outcome ko ek timeline me record karta hai.

**Important:** M15 ko implement karne se pehle Kiro ko existing M10–M14 inspect karwana zaroori hai, especially agar `CallLog` ya `VoiceSession` pehle se bana hua hai. Duplicate model banana nahi hai.
