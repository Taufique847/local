Bilkul. **M12 ko ab actual Lead Automation Engine** rakhenge. M11/M11.2 ke tools already available hain; M12 un tools ko business workflow mein use karke **AI call → lead qualification → lead creation/update → lifecycle → follow-up-ready state** banayega.

Is phase mein main **lead ko sirf MongoDB document create karne tak limit nahi kar raha**. Production SaaS ke liye lead lifecycle, qualification, duplicate handling, source attribution, AI confidence, urgency, service type, activity history aur automation hooks bhi define karenge.

# M12 — LEAD AUTOMATION ENGINE

## Complete Detailed Master Prompt for Kiro AI

```text id="8v3kpq"
You are a Principal SaaS Architect, AI Automation Engineer,
Backend Engineer, CRM Architect, Data Model Designer,
Realtime Voice AI Engineer, and Security Engineer.

You are continuing an existing production-oriented
multi-tenant HVAC AI Employee SaaS.

============================================================
MODULE
============================================================

MODULE: M12

NAME:
LEAD AUTOMATION ENGINE

PRIMARY OBJECTIVE:

Convert customer conversations handled by the AI receptionist
into structured, qualified, tenant-isolated HVAC leads.

M12 must connect:

VOICE CONVERSATION
        ↓
CUSTOMER INTENT
        ↓
INFORMATION COLLECTION
        ↓
LEAD QUALIFICATION
        ↓
LEAD CREATION / UPDATE
        ↓
LEAD LIFECYCLE
        ↓
LEAD ACTIVITY
        ↓
AUTOMATION-READY EVENTS

============================================================
IMPORTANT EXISTING MODULES
============================================================

M10:
Basic AI Voice Engine

M11:
AI Tool Calling Engine

M11.2:
Tool Execution Reliability & Control Layer

M12 must build on these.

DO NOT rebuild them.

============================================================
STRICT NON-REBUILD RULE
============================================================

Before writing code:

1. Inspect M1-M11.2.
2. Inspect existing Lead model.
3. Inspect Customer model.
4. Inspect Business model.
5. Inspect VoiceSession.
6. Inspect Tool Registry.
7. Inspect Tool Executor.
8. Inspect Tool Context.
9. Inspect AI provider abstraction.
10. Inspect existing authentication.
11. Inspect existing tenant middleware.
12. Inspect existing logger.
13. Inspect existing error handler.
14. Inspect existing event system.
15. Inspect existing API conventions.

Reuse existing implementations.

Do NOT create duplicate:

- Lead model
- Customer model
- Business model
- tool executor
- tenant middleware
- authentication middleware
- logger
- error handler

============================================================
1. M12 PRODUCT GOAL
============================================================

The AI receptionist should be capable of turning a call into
a useful business lead.

Example:

Customer calls.

Customer:
"My AC is running but the house isn't getting cold."

AI:
understands HVAC repair intent.

AI collects:

Name
Phone
Service address
Problem
Urgency
Service type

Then:

AI
 ↓
create/update customer
 ↓
create/update lead
 ↓
qualify lead
 ↓
save lead
 ↓
record activity
 ↓
return result to AI

The AI can then continue the conversation.

============================================================
2. LEAD DEFINITION
============================================================

A Lead represents a potential HVAC service/sales opportunity.

Examples:

- AC repair request
- Heating repair request
- HVAC maintenance request
- New AC installation inquiry
- HVAC replacement inquiry
- Emergency service request
- General service inquiry

A lead is NOT the same as:

Customer

Appointment

Call

Conversation

Invoice

Subscription

============================================================
3. CUSTOMER VS LEAD
============================================================

Customer:

Who the person is.

Lead:

What business opportunity/service request they currently have.

One customer may have:

multiple leads over time.

Example:

Customer:
John Smith

Lead 1:
AC repair — June

Lead 2:
Heating repair — December

Therefore:

DO NOT create a new customer for every lead.

============================================================
4. LEAD LIFECYCLE
============================================================

Implement a clear lifecycle.

Recommended statuses:

NEW
CONTACTED
QUALIFIED
UNQUALIFIED
APPOINTMENT_PENDING
APPOINTMENT_BOOKED
IN_PROGRESS
COMPLETED
LOST
CANCELLED

Do not create unnecessary statuses.

============================================================
5. STATUS TRANSITION RULES
============================================================

Define valid transitions.

Example:

NEW
 ↓
CONTACTED
 ↓
QUALIFIED
 ↓
APPOINTMENT_PENDING
 ↓
APPOINTMENT_BOOKED
 ↓
IN_PROGRESS
 ↓
COMPLETED

Alternative:

NEW
 ↓
UNQUALIFIED

or:

QUALIFIED
 ↓
LOST

or:

APPOINTMENT_BOOKED
 ↓
CANCELLED

Do not allow arbitrary status jumps.

============================================================
6. STATE MACHINE
============================================================

Create a Lead State Machine.

Concept:

LeadStateMachine.transition(
  currentStatus,
  nextStatus,
  context
)

It should verify:

- valid transition
- business ownership
- permissions
- required data
- source
- actor

If invalid:

return structured business error.

============================================================
7. LEAD MODEL
============================================================

Inspect existing Lead model first.

If insufficient, extend it.

Recommended conceptual fields:

{
  _id,

  businessId,

  customerId,

  source,

  status,

  intent,

  serviceType,

  issueDescription,

  urgency,

  qualification,

  contact,

  serviceAddress,

  preferredDate,

  preferredTimeRange,

  aiMetadata,

  assignedTo,

  appointmentId,

  lastContactedAt,

  nextFollowUpAt,

  createdAt,

  updatedAt
}

Do not blindly implement every field if existing architecture
already provides equivalent structures.

============================================================
8. TENANT OWNERSHIP
============================================================

Every lead MUST contain:

businessId

Every query MUST be tenant scoped.

Correct:

Lead.find({
  businessId: context.businessId
})

Never:

Lead.find({})

for business-level operations.

Never allow LLM input to provide authoritative businessId.

Backend derives it from ToolContext.

============================================================
9. CUSTOMER RELATIONSHIP
============================================================

Lead must reference customerId.

Before attaching customer:

verify:

customer.businessId === lead.businessId

Never attach a customer from another tenant.

============================================================
10. SOURCE ATTRIBUTION
============================================================

Track lead source.

Possible values:

AI_VOICE
PHONE
WEB
MANUAL
REFERRAL
IMPORT
OTHER

For M12 AI-created voice leads:

source = AI_VOICE

Do not trust the LLM to set arbitrary source values.

Backend should derive source from context.

============================================================
11. LEAD INTENT
============================================================

Define controlled intent values.

Recommended:

AC_REPAIR
HEATING_REPAIR
THERMOSTAT
AIRFLOW
REFRIGERANT
WATER_LEAK
NOISE
ODOR
MAINTENANCE
INSTALLATION
REPLACEMENT
EMERGENCY
GENERAL_INQUIRY

Keep intent extensible.

Do not allow arbitrary unlimited strings.

============================================================
12. SERVICE TYPE
============================================================

Separate:

intent

from:

serviceType

Example:

intent:
AC_REPAIR

serviceType:
REPAIR

Possible service types:

REPAIR
MAINTENANCE
INSTALLATION
REPLACEMENT
INSPECTION
EMERGENCY
OTHER

============================================================
13. URGENCY
============================================================

Define:

LOW
NORMAL
HIGH
EMERGENCY

The AI may suggest urgency.

Backend stores the normalized value.

Do not let the AI arbitrarily mark everything as EMERGENCY.

============================================================
14. URGENCY RULES
============================================================

Create basic deterministic rules.

Examples:

"No AC and it's 100 degrees"

may indicate higher urgency.

"Strange smell from equipment"

requires cautious handling.

"Smoke/fire"

must not be treated as a normal HVAC service lead.

For dangerous situations:

the AI should advise appropriate emergency services when warranted.

Do not make medical/legal/safety claims outside the system's scope.

============================================================
15. QUALIFICATION
============================================================

Implement Lead Qualification.

Qualification should be structured.

Example:

{
  status,
  score,
  reasons,
  missingInformation
}

Possible qualification status:

UNQUALIFIED
PARTIALLY_QUALIFIED
QUALIFIED

Do NOT make qualification score the sole source of truth.

============================================================
16. LEAD SCORE
============================================================

Implement an explainable lead score.

Example factors:

Intent clarity
Service need
Contact information
Service address
Urgency
Purchase/repair intent
Appointment interest

Example:

score: 0–100

Do NOT claim that the score predicts revenue with certainty.

Score is an internal prioritization signal.

============================================================
17. SCORE TRANSPARENCY
============================================================

Store scoring factors.

Example:

{
  score: 78,
  factors: [
    {
      name: "clear_service_need",
      points: 20
    },
    {
      name: "service_address",
      points: 15
    }
  ]
}

Do not store hidden reasoning or chain-of-thought.

Store concise business-relevant reasons only.

============================================================
18. REQUIRED LEAD INFORMATION
============================================================

Define required fields for qualification.

Minimum:

customer name
phone number
service need

For service scheduling:

service address

Potential additional:

issue description
urgency
preferred time

Do not force unnecessary questions.

============================================================
19. INFORMATION COMPLETENESS
============================================================

Create:

LeadCompletenessService

It should calculate:

missing fields

completed fields

qualification readiness

Example:

{
  complete: false,
  missing: [
    "serviceAddress"
  ]
}

============================================================
20. AI CONVERSATION INTEGRATION
============================================================

M12 must integrate with M10/M11.

Conversation:

Customer:
"My AC isn't cooling."

AI:

collects information.

Tool:

find_customer

If not found:

create_customer

Then:

create_lead

Then:

update_lead

as more information becomes available.

============================================================
21. CREATE LEAD TOOL
============================================================

Reuse M11 create_lead tool if already implemented.

Do NOT create a second create_lead tool.

Extend it to support M12 fields where appropriate.

Potential input:

{
  customerId,
  intent,
  serviceType,
  issueDescription,
  serviceAddress,
  urgency
}

Backend adds:

businessId
source
createdAt

============================================================
22. UPDATE LEAD TOOL
============================================================

Reuse M11 update_lead.

Allow updates to:

issueDescription
serviceAddress
urgency
intent
serviceType
preferredDate
preferredTimeRange

Only explicitly allowed fields.

============================================================
23. LEAD DEDUPLICATION
============================================================

This is critical.

AI may call create_lead multiple times during one call.

Do not create duplicates.

Possible duplicate matching:

businessId
+
customerId
+
active lead
+
same/relevant service intent

Use business rules.

Do not merge unrelated service requests.

============================================================
24. MULTIPLE ACTIVE LEADS
============================================================

A customer may legitimately have multiple service needs.

Example:

"AC is not cooling and I also want a new furnace."

Do not blindly merge.

The system should determine whether:

one combined lead

or

multiple leads

is appropriate based on configured business rules.

For M12:

prefer one lead for one current service conversation unless
there is a clear distinct opportunity.

============================================================
25. CALL-TO-LEAD ASSOCIATION
============================================================

A lead created from a call should preserve source context.

If existing architecture supports it:

store:

sourceCallId
sessionId

or create an activity linking the lead to the call.

Do not duplicate complete call transcript.

Detailed transcript belongs to M15.

============================================================
26. LEAD ACTIVITY
============================================================

Create a lightweight Lead Activity system.

Activity examples:

LEAD_CREATED
LEAD_UPDATED
STATUS_CHANGED
QUALIFIED
UNQUALIFIED
CALL_RECEIVED
CUSTOMER_INFORMATION_CAPTURED
APPOINTMENT_REQUESTED
APPOINTMENT_BOOKED
FOLLOW_UP_REQUIRED

Each activity should include:

businessId
leadId
type
timestamp
actorType

Actor types:

AI
CUSTOMER
USER
SYSTEM

============================================================
27. ACTIVITY ACTOR
============================================================

For AI-created lead:

actorType = AI

For dashboard employee:

actorType = USER

For automated state:

actorType = SYSTEM

Do not store fake employee IDs for AI actions.

============================================================
28. ACTIVITY IMMUTABILITY
============================================================

Activities should generally be append-only.

Do not allow normal updates to historical activity records.

If correction is required:

create a new corrective activity.

============================================================
29. LEAD TIMELINE
============================================================

Provide a service to retrieve:

lead details

+
chronological activities

This will later power the dashboard.

============================================================
30. LEAD API
============================================================

Create or extend API routes using existing route conventions.

Conceptual:

GET /api/leads

GET /api/leads/:id

POST /api/leads

PATCH /api/leads/:id

POST /api/leads/:id/status

GET /api/leads/:id/activities

Do not create duplicate routes if M11 already created them.

============================================================
31. GET LEADS
============================================================

Support tenant-scoped filtering.

Possible filters:

status
intent
serviceType
urgency
source
assignedTo
date range
search

Never allow:

businessId from query string

to override authenticated tenant.

BusinessId must come from authenticated context.

============================================================
32. LEAD SEARCH
============================================================

Search may include:

customer name
phone
lead ID where appropriate

Use safe indexed queries.

Do not expose arbitrary MongoDB search operators.

============================================================
33. PAGINATION
============================================================

GET /api/leads must support pagination.

Example:

page
limit

or cursor pagination if existing architecture uses it.

Apply reasonable maximum page size.

Do not allow:

limit=100000

============================================================
34. SORTING
============================================================

Allow only whitelisted sort fields.

Example:

createdAt
updatedAt
score
nextFollowUpAt

Do not allow arbitrary field sorting from request input.

============================================================
35. LEAD DETAIL
============================================================

GET /api/leads/:id must verify:

lead exists

lead.businessId === authenticated business

Then return:

customer summary
lead information
qualification
activity summary
appointment reference if available

Do not expose internal/private fields.

============================================================
36. LEAD CREATION API
============================================================

Manual dashboard users may create leads.

Validate:

customer
intent
serviceType
address
urgency

Set:

businessId from authenticated context.

Do not trust frontend businessId.

============================================================
37. LEAD UPDATE API
============================================================

Allow only approved fields.

Validate every field.

Do not allow frontend to modify:

businessId
createdAt
source
audit history

unless explicitly supported.

============================================================
38. STATUS UPDATE API
============================================================

Use:

POST /api/leads/:id/status

Body:

{
  status,
  reason
}

Validate state transition.

Record activity:

STATUS_CHANGED

Include actorType:

USER

============================================================
39. AI STATUS UPDATE
============================================================

AI may update status only through authorized tools.

Example:

NEW → CONTACTED

QUALIFIED

APPOINTMENT_PENDING

Do not allow AI to directly mark:

COMPLETED

unless actual business event confirms completion.

============================================================
40. QUALIFICATION RULE ENGINE
============================================================

Create:

leadQualification.service.js

Inputs:

lead data

Outputs:

qualification status

score

missing information

factors

Keep it deterministic where possible.

Do not make qualification dependent entirely on LLM judgment.

============================================================
41. QUALIFICATION EXAMPLE
============================================================

Example:

Name:
present

Phone:
present

Issue:
present

Address:
present

Appointment intent:
present

Result:

QUALIFIED

If:

Name present
Phone present
Issue present
Address missing

Result:

PARTIALLY_QUALIFIED

============================================================
42. AI CONFIDENCE
============================================================

If the AI provider returns confidence metadata and the existing
architecture supports it:

store only normalized confidence.

Example:

intentConfidence

Do not fabricate confidence values.

If unavailable:

leave null.

============================================================
43. CONFLICT RESOLUTION
============================================================

If AI says:

intent = AC_REPAIR

but conversation context later indicates:

installation

do not blindly overwrite without updated evidence.

Use:

latest validated information

and activity history.

============================================================
44. LEAD OWNERSHIP / ASSIGNMENT
============================================================

M12 may support:

assignedTo

but do NOT build a complete assignment engine yet.

For now:

allow nullable assignedTo.

Future module may implement:

round robin

technician assignment

team assignment

AI assignment

============================================================
45. NEXT FOLLOW-UP
============================================================

M12 can store:

nextFollowUpAt

but should NOT implement full follow-up automation yet.

This field prepares M14+ automation.

============================================================
46. FOLLOW-UP READY EVENTS
============================================================

When lead reaches certain states:

emit an internal event.

Example:

LEAD_CREATED

LEAD_QUALIFIED

LEAD_NEEDS_FOLLOW_UP

Do not send SMS/email automatically in M12 unless explicitly required
by an existing module.

M14 will own full communication automation.

============================================================
47. EVENT SYSTEM
============================================================

If an existing event bus exists:

reuse it.

Otherwise create a lightweight domain event mechanism.

Events:

lead.created
lead.updated
lead.status_changed
lead.qualified
lead.unqualified
lead.follow_up_required

Events should include:

businessId
leadId
timestamp
source

Do not include huge payloads.

============================================================
48. EVENT IDEMPOTENCY
============================================================

Events must not accidentally create duplicate downstream actions.

Include:

eventId

Consumers can later use idempotency.

============================================================
49. LEAD CREATION FROM VOICE
============================================================

Voice flow:

Incoming call
 ↓
Voice session
 ↓
AI conversation
 ↓
Customer information
 ↓
find_customer
 ↓
create_customer if needed
 ↓
create_lead
 ↓
qualification
 ↓
lead activity
 ↓
AI continues

============================================================
50. WHEN TO CREATE A LEAD
============================================================

Do not create a lead immediately after:

"Hello."

A lead should be created when there is a meaningful business/service intent.

Example:

"I need someone to repair my AC."

This qualifies as a lead opportunity.

============================================================
51. WHEN NOT TO CREATE A LEAD
============================================================

Examples:

Wrong number

Vendor solicitation

Spam

General greeting

Caller only asks for unrelated information

No meaningful service/sales intent

Do not create meaningless leads.

============================================================
52. UNKNOWN INTENT
============================================================

If intent is unclear:

do not fabricate intent.

Possible:

GENERAL_INQUIRY

or keep intent null until clarified.

============================================================
53. EMERGENCY LEADS
============================================================

If urgency is EMERGENCY:

lead should be marked accordingly.

But:

Do NOT claim emergency dispatch.

Do NOT claim technician dispatched.

Do NOT claim guaranteed response time.

Only actual backend operations can confirm actions.

============================================================
54. LEAD QUALITY
============================================================

Create a lead quality classification.

Example:

HIGH_SIGNAL
MEDIUM_SIGNAL
LOW_SIGNAL

This is different from status.

Do not use quality classification as an absolute prediction.

============================================================
55. DATA NORMALIZATION
============================================================

Normalize:

phone numbers

addresses where existing infrastructure supports it

enum values

timestamps

Do not aggressively modify customer-entered addresses.

============================================================
56. TIMEZONE
============================================================

Lead dates/times must respect business timezone.

Do not assume:

UTC

India timezone

or browser timezone

for US business scheduling.

Use Business.timezone if existing.

If timezone missing:

use configured default.

Do not silently choose based on server location.

============================================================
57. DATE HANDLING
============================================================

AI may say:

"tomorrow"

Backend should resolve relative dates using:

business timezone
+
conversation timestamp

Never resolve "tomorrow" using server timezone blindly.

============================================================
58. LEAD API SECURITY
============================================================

Require authentication for dashboard APIs.

Apply:

auth middleware

tenant middleware

RBAC where available

Validate resource ownership.

============================================================
59. AI TOOL SECURITY
============================================================

AI tool calls must use:

ToolContext.businessId

not model-supplied businessId.

Tool executor from M11.2 remains authoritative.

============================================================
60. LEAD DATA PRIVACY
============================================================

Do not store:

raw voice audio

unnecessary sensitive information

LLM chain-of-thought

system prompt

API keys

unnecessary customer data

============================================================
61. PII LOGGING
============================================================

Logs should not contain full customer PII unnecessarily.

Prefer:

customerId

leadId

businessId

sessionId

over:

full address

full phone

full conversation

============================================================
62. INDEXES
============================================================

Inspect current Lead model.

Add appropriate indexes.

Potential:

businessId + createdAt

businessId + status

businessId + customerId

businessId + source

businessId + nextFollowUpAt

Do not add every index blindly.

Index based on actual queries.

============================================================
63. DUPLICATE INDEX STRATEGY
============================================================

Do not create a global unique customerId index on leads.

One customer may legitimately have multiple leads.

Use business rules for active lead deduplication.

============================================================
64. DATA VALIDATION
============================================================

Use existing project validation.

Validate:

ObjectIds

enum values

dates

strings

phone

addresses

status transitions

============================================================
65. ERROR HANDLING
============================================================

Standard errors:

LEAD_NOT_FOUND

INVALID_LEAD_DATA

INVALID_STATUS_TRANSITION

TENANT_MISMATCH

CUSTOMER_NOT_FOUND

DUPLICATE_LEAD

LEAD_ALREADY_CLOSED

QUALIFICATION_FAILED

INVALID_INTENT

INVALID_SERVICE_TYPE

UNAUTHORIZED

============================================================
66. AI-FACING ERRORS
============================================================

Never expose internal database errors.

Example:

Internal:

Mongo duplicate key error

AI receives:

"That service request is already recorded."

============================================================
67. CUSTOMER-FACING RESPONSE
============================================================

The AI should speak naturally.

Example after successful lead creation:

"Thanks. I've got the details for your AC service request."

Do NOT say:

"MongoDB lead document created successfully."

============================================================
68. LEAD QUALIFICATION CONVERSATION
============================================================

The AI should ask only necessary missing questions.

Example:

Customer:
"My AC isn't cooling."

AI:
"Got it. Can I get the service address?"

After address:

"Thanks. And what name should I put on the service request?"

Avoid:

asking 10 questions unnecessarily.

============================================================
69. CONVERSATIONAL MEMORY
============================================================

Use M10 conversation context.

Do not ask again for:

name

phone

address

if already reliably captured.

============================================================
70. INFORMATION CONFIDENCE
============================================================

Distinguish:

explicitly provided

inferred

unknown

For critical fields:

prefer explicit customer-provided information.

Example:

Service address should not be guessed.

============================================================
71. LEAD MERGING
============================================================

If duplicate active lead detected:

return existing lead.

Optionally update missing information.

Do not silently merge unrelated requests.

Record activity if existing lead was enriched.

============================================================
72. LEAD CLOSURE
============================================================

A lead may become:

COMPLETED

when actual completion event is available.

M12 should NOT invent completion.

If no completion event exists:

do not automatically mark completed.

============================================================
73. LOST LEAD
============================================================

LOST should require a reason where appropriate.

Examples:

CUSTOMER_DECLINED

NO_RESPONSE

OUTSIDE_SERVICE_AREA

PRICE_DECLINED

OTHER

Use controlled values.

============================================================
74. UNQUALIFIED REASON
============================================================

Examples:

NO_SERVICE_INTENT

INVALID_CONTACT

OUTSIDE_SERVICE_AREA

INSUFFICIENT_INFORMATION

SPAM

WRONG_NUMBER

Do not expose internal classifications unnecessarily to customers.

============================================================
75. SERVICE AREA
============================================================

If Business model has service area:

use it.

If existing service-area validation exists:

reuse it.

If not:

DO NOT build a complete geospatial service-area system in M12.

Leave extensibility.

============================================================
76. LEAD AUTOMATION HOOKS
============================================================

Create hooks for future:

appointment automation

SMS automation

follow-up automation

human handoff

analytics

Example:

LeadCreatedEvent

LeadQualifiedEvent

LeadNeedsFollowUpEvent

============================================================
77. EVENT PAYLOAD
============================================================

Example:

{
  eventId,
  type: "lead.qualified",
  businessId,
  leadId,
  occurredAt,
  source
}

Keep payload small.

Consumers can query detailed lead data.

============================================================
78. DASHBOARD READINESS
============================================================

Backend should expose enough data for future dashboard.

Lead list should support:

status

intent

urgency

score

source

createdAt

nextFollowUpAt

customer summary

============================================================
79. FRONTEND — LEAD LIST
============================================================

If the existing dashboard lead UI does not exist yet,
M12 may create a basic lead management page.

Do NOT redesign the whole dashboard.

Create:

/dashboard/leads

Features:

- list leads
- pagination
- search
- status filter
- urgency filter
- source filter
- basic sorting
- open lead detail

============================================================
80. LEAD DETAIL UI
============================================================

Create:

/dashboard/leads/[id]

Display:

Customer
Phone
Service address
Intent
Service type
Urgency
Status
Qualification
Score
Created time
Last update
Appointment reference if available
Activity timeline

============================================================
81. UI DESIGN
============================================================

Use the existing design system.

Do not create random styles.

The UI must feel like a professional HVAC SaaS dashboard.

Use:

cards

tables

badges

filters

empty states

loading states

error states

responsive layout

desktop sidebar

mobile-friendly layout

============================================================
82. LEAD STATUS UI
============================================================

Status badges should be visually distinct.

Do not rely only on color.

Include text labels.

Example:

New

Qualified

Appointment Pending

Booked

In Progress

Completed

Lost

Cancelled

============================================================
83. LEAD EMPTY STATE
============================================================

If no leads:

show useful message.

Example:

"No leads yet. Leads created through AI calls and your other
connected channels will appear here."

Do not show broken blank screen.

============================================================
84. LOADING STATE
============================================================

Use existing skeleton/loading components if available.

Do not block entire dashboard unnecessarily.

============================================================
85. ERROR STATE
============================================================

Show:

"Unable to load leads."

Provide retry action.

Do not show raw API errors.

============================================================
86. MOBILE UI
============================================================

Lead list must work on mobile.

Avoid huge desktop-only tables.

Use responsive cards or horizontal scrolling where appropriate.

============================================================
87. LEAD DETAIL ACTIVITY TIMELINE
============================================================

Display activities chronologically.

Example:

09:32
AI created lead

09:34
Customer information captured

09:36
Lead qualified

09:38
Appointment requested

Do not expose internal technical events to normal users.

============================================================
88. ROLE-BASED UI
============================================================

Respect existing roles.

For example:

Owner/Admin:
can manage leads

Staff:
may view/update based on permission

AI:
not a frontend user

Do not create a fake user account for AI.

============================================================
89. API TESTS
============================================================

Test:

GET leads

GET lead

POST lead

PATCH lead

status transition

activities

pagination

filters

tenant isolation

authorization

============================================================
90. TOOL TESTS
============================================================

Test:

create_lead

update_lead

duplicate detection

qualification

cross-tenant access

invalid intent

invalid service type

invalid urgency

============================================================
91. STATE MACHINE TESTS
============================================================

Test valid transitions.

Test invalid:

COMPLETED → NEW

CANCELLED → QUALIFIED

LOST → APPOINTMENT_BOOKED

unless explicitly supported.

============================================================
92. QUALIFICATION TESTS
============================================================

Test:

complete lead

missing address

missing contact

unclear intent

high urgency

emergency

installation inquiry

general inquiry

============================================================
93. DUPLICATE TESTS
============================================================

Test:

same customer
same intent
same active lead

Expected:

existing lead reused/enriched.

Different service intent:

Expected:

may create separate lead according to business rule.

============================================================
94. MULTI-TENANT TEST
============================================================

Business A:

Lead A

Business B:

Lead B

Business A must never retrieve/update Lead B.

Test through:

API

tool

service

direct service invocation where possible

============================================================
95. CONCURRENT CREATION TEST
============================================================

Simulate:

two identical create_lead calls simultaneously.

Expected:

no unintended duplicate active lead.

Use appropriate:

atomic operation

unique/idempotency strategy

or existing M11.2 infrastructure.

============================================================
96. EVENT TESTS
============================================================

Verify:

lead.created

lead.updated

lead.status_changed

lead.qualified

lead.follow_up_required

Events have:

eventId

businessId

leadId

timestamp

============================================================
97. REGRESSION TESTS
============================================================

Verify:

M10 voice still works.

M11 tool calling still works.

M11.2 reliability still works.

AI voice lead creation works.

No existing authentication breaks.

No existing onboarding breaks.

============================================================
98. PERFORMANCE
============================================================

Lead queries should use indexes.

Avoid:

N+1 queries

loading entire activity history unnecessarily

loading full customer documents

unbounded lists

unbounded activity responses

============================================================
99. PAGINATION FOR ACTIVITIES
============================================================

Lead activity timeline should support pagination if activity volume
can grow.

Do not load thousands of activities on every detail page.

============================================================
100. DATABASE MIGRATION
============================================================

If schema changes are required:

provide migration/index instructions.

Do not destroy existing lead data.

Never drop production collections.

Never run destructive migration automatically.

============================================================
101. BACKWARD COMPATIBILITY
============================================================

Existing M11 tools must continue working.

If create_lead input changes:

maintain backward compatibility where practical.

If breaking change is unavoidable:

document exact migration.

============================================================
102. NO PREMATURE M13 FEATURES
============================================================

M12 MUST NOT implement complete appointment automation.

Do not implement:

technician assignment

calendar synchronization

advanced scheduling engine

appointment reminders

rescheduling workflow

cancellation automation

These belong to M13+.

M12 may only store:

appointment reference

appointment requested state

events/hooks.

============================================================
103. NO PREMATURE M14 FEATURES
============================================================

Do not implement:

SMS campaign system

follow-up sequences

email automation

marketing campaigns

automated reminders

Those belong to later communication modules.

============================================================
104. NO PREMATURE M15 FEATURES
============================================================

Do not implement complete:

transcript storage

call analytics

sentiment analysis

call recording management

Those belong to M15.

============================================================
105. NO PREMATURE AI KNOWLEDGE BASE
============================================================

Do not implement RAG or knowledge base in M12.

============================================================
106. NO CHAIN OF THOUGHT STORAGE
============================================================

Never store hidden model reasoning.

Only store:

structured business-relevant metadata.

============================================================
107. AUDITABILITY
============================================================

Every important lead change should be traceable through activities.

Examples:

who/what created it

who changed status

when it changed

source

Do not allow history rewriting through normal APIs.

============================================================
108. DATA CONSISTENCY
============================================================

When creating a lead:

customer must exist.

If customer creation and lead creation are separate operations:

handle failure safely.

Do not create a lead referencing a customer that doesn't exist.

============================================================
109. CUSTOMER + LEAD FLOW
============================================================

If customer doesn't exist:

create customer

then create lead.

If customer exists:

reuse customer.

If create_customer succeeds but create_lead fails:

do not create duplicate customer on retry.

M11.2 idempotency must protect this flow.

============================================================
110. VOICE SESSION FINALIZATION
============================================================

M12 should expose a clean hook:

onVoiceSessionLeadUpdate()

or equivalent existing event.

Do not tightly couple Lead service to WebSocket implementation.

============================================================
111. DOMAIN EVENTS
============================================================

Preferred architecture:

Voice Layer
 ↓
AI Tool
 ↓
Lead Service
 ↓
Lead Event
 ↓
Future Automation

Not:

Voice Layer
 ↓
Lead Service
 ↓
Twilio SMS
 ↓
Calendar
 ↓
Email
 ↓
Everything

Keep modules decoupled.

============================================================
112. API DOCUMENTATION
============================================================

Document:

GET /api/leads

GET /api/leads/:id

POST /api/leads

PATCH /api/leads/:id

POST /api/leads/:id/status

GET /api/leads/:id/activities

For each:

request

response

validation

authorization

errors

============================================================
113. EXAMPLE LEAD OBJECT
============================================================

Example sanitized API response:

{
  "id": "...",
  "customer": {
    "id": "...",
    "name": "John Smith",
    "phoneNumber": "..."
  },
  "status": "QUALIFIED",
  "intent": "AC_REPAIR",
  "serviceType": "REPAIR",
  "urgency": "HIGH",
  "issueDescription": "AC is running but not cooling",
  "serviceAddress": "...",
  "source": "AI_VOICE",
  "qualification": {
    "status": "QUALIFIED",
    "score": 82,
    "missingInformation": []
  },
  "createdAt": "...",
  "updatedAt": "..."
}

Do not expose unnecessary internal fields.

============================================================
114. AI TOOL RESULT
============================================================

Example:

{
  "success": true,
  "data": {
    "leadId": "...",
    "status": "QUALIFIED",
    "created": true
  }
}

Keep AI tool result concise.

============================================================
115. AI RESPONSE
============================================================

After successful lead creation:

"Thanks. I've got your service request details."

Do not expose internal lead ID unless needed.

============================================================
116. TOOL FAILURE
============================================================

If create_lead fails:

AI should say:

"I'm having trouble saving those details right now. Let me make
sure I have the information correctly."

Do not say:

"MongoDB returned error code 11000."

============================================================
117. LEAD AUTOMATION FUTURE HOOK
============================================================

When lead becomes QUALIFIED:

emit:

lead.qualified

Future modules can subscribe.

M12 itself should not automatically send SMS.

============================================================
118. SERVICE AREA FUTURE HOOK
============================================================

If lead appears outside configured service area:

support:

OUTSIDE_SERVICE_AREA

but do not build advanced maps/geospatial logic unless existing.

============================================================
119. SPAM / WRONG NUMBER
============================================================

Allow safe classification.

If clear:

WRONG_NUMBER

or

SPAM

do not create normal qualified lead.

If a lead was already created:

it may transition to:

UNQUALIFIED

with appropriate reason.

============================================================
120. BUSINESS RULE CONFIGURATION
============================================================

Do not hardcode every business rule.

Where architecture permits:

store configurable business settings.

However:

do not build a full configuration UI in M12.

============================================================
121. TEST DATA
============================================================

Create development/test fixtures:

HVAC business

customers

leads

lead activities

Do not use real personal data.

============================================================
122. SEED DATA
============================================================

If existing seed system exists:

extend it.

Example statuses:

NEW
CONTACTED
QUALIFIED
APPOINTMENT_PENDING
APPOINTMENT_BOOKED
COMPLETED
LOST

============================================================
123. DOCUMENTATION
============================================================

Update README:

M12 overview

lead lifecycle

lead model

qualification

lead scoring

tool integration

API endpoints

events

tenant isolation

testing

============================================================
124. ARCHITECTURE DOCUMENT
============================================================

Document:

AI Voice
 ↓
Tool Calling
 ↓
Lead Service
 ↓
Qualification
 ↓
State Machine
 ↓
Lead DB
 ↓
Activities
 ↓
Domain Events

============================================================
125. SECURITY CHECKLIST
============================================================

Verify:

[ ] tenant isolation
[ ] authenticated API
[ ] RBAC
[ ] server-owned businessId
[ ] strict input validation
[ ] safe ObjectId handling
[ ] no raw Mongo operators
[ ] no arbitrary update fields
[ ] no raw database errors
[ ] PII-safe logging
[ ] immutable activity history
[ ] AI cannot bypass permissions

============================================================
126. FINAL ACCEPTANCE CRITERIA
============================================================

M12 is complete only when:

[ ] Lead model is production-ready
[ ] Lead lifecycle exists
[ ] Lead state machine exists
[ ] Valid transitions are enforced
[ ] Lead intent taxonomy exists
[ ] Service type exists
[ ] Urgency exists
[ ] Lead qualification exists
[ ] Explainable lead score exists
[ ] Missing information detection exists
[ ] Customer relationship exists
[ ] Lead source attribution exists
[ ] AI voice source works
[ ] Lead deduplication exists
[ ] Lead activity system exists
[ ] Activity timeline exists
[ ] Lead APIs exist
[ ] Tenant isolation works
[ ] RBAC works
[ ] AI tools integrate correctly
[ ] Duplicate tool calls are safe
[ ] Concurrent lead creation is safe
[ ] Domain events exist
[ ] Follow-up hooks exist
[ ] Basic lead dashboard exists if required
[ ] Lead detail page exists if required
[ ] Mobile UI works
[ ] Loading states work
[ ] Empty states work
[ ] Error states work
[ ] API tests pass
[ ] Tool tests pass
[ ] State machine tests pass
[ ] Qualification tests pass
[ ] Tenant security tests pass
[ ] Concurrency tests pass
[ ] M10 regression passes
[ ] M11 regression passes
[ ] M11.2 regression passes
[ ] README updated
[ ] No M13/M14/M15 features were prematurely implemented

============================================================
127. FINAL IMPLEMENTATION REPORT
============================================================

After implementation provide a detailed report:

1. Existing architecture discovered
2. Existing Lead model status
3. Files created
4. Files modified
5. Files reused
6. Lead data model
7. Lead lifecycle
8. State machine
9. Status transitions
10. Intent taxonomy
11. Service types
12. Urgency system
13. Qualification engine
14. Lead scoring
15. Duplicate detection
16. Customer relationship
17. Lead activity system
18. API endpoints
19. Tool integration
20. AI voice integration
21. Tenant isolation
22. RBAC
23. Event system
24. Idempotency
25. Concurrency protection
26. Database indexes
27. Frontend pages
28. Responsive behavior
29. Tests
30. Security tests
31. Manual voice tests
32. Environment changes
33. Migration requirements
34. Known limitations
35. M10 regression result
36. M11 regression result
37. M11.2 regression result
38. Confirmation that M13+ was NOT prematurely implemented

============================================================
FINAL RULE
============================================================

Do not claim a lead was created unless the database operation succeeded.

Do not claim a lead is qualified unless the qualification engine
actually determined it.

Do not claim an appointment was booked unless M13 appointment
system confirms it.

Do not send SMS from M12 unless an existing explicitly authorized
communication capability requires it.

Do not fabricate data.

Do not store chain-of-thought.

Do not expose secrets.

Do not bypass tenant isolation.

START NOW.

FIRST inspect M1-M11.2.

THEN map existing Lead/Customer/Business/Voice/Tool architecture.

THEN create an implementation plan.

THEN implement M12 incrementally.

THEN run tests.

THEN run regression tests.

THEN provide the detailed implementation report.
```

## M12 ke baad system mein kya add hoga?

Ab tumhara flow significantly mature ho jayega:

```text
                    CUSTOMER
                       │
                       ▼
                     TWILIO
                       │
                       ▼
                 M10 VOICE AI
                       │
                       ▼
              M11 TOOL CALLING
                       │
                       ▼
            M11.2 RELIABILITY
                       │
                       ▼
              M12 LEAD ENGINE
                       │
          ┌────────────┼────────────┐
          ▼            ▼            ▼
     CUSTOMER       LEAD       QUALIFICATION
          │            │            │
          │            ▼            ▼
          │       STATE MACHINE    SCORE
          │            │            │
          └────────────┼────────────┘
                       ▼
                 LEAD ACTIVITY
                       │
                       ▼
                DOMAIN EVENTS
                       │
             ┌─────────┼─────────┐
             ▼         ▼         ▼
       Appointment   SMS      Follow-up
         Future      Future     Future
```

### Example: actual HVAC call

Customer:

> "Hi, my AC has been running all day but my house is still hot."

AI M10 conversation layer issue samjhega.

M11 tool layer:

```text
find_customer()
```

Customer nahi mila:

```text
create_customer()
```

Phir M12:

```text
create_lead()
```

Lead:

```text
status: NEW
intent: AC_REPAIR
serviceType: REPAIR
urgency: HIGH
source: AI_VOICE
```

Customer address deta hai.

AI:

```text
update_lead()
```

Qualification engine:

```text
name              ✓
phone             ✓
service need      ✓
issue description ✓
address            ✓
```

Result:

```text
QUALIFIED
score: 82
```

Activity timeline:

```text
05:42  AI created lead
05:43  Customer information captured
05:44  Service address captured
05:44  Lead qualified
```

Aur **M12 yahin rukega**. Appointment actually book karna **M13** ka kaam hoga. Isi separation se architecture clean rahega.
