Bilkul. Ab **M14 = Communication & SMS Automation Engine** rakhenge.

M13 ke baad logical flow hoga:

**Call → Lead → Qualification → Availability → Appointment → M14 Communication**

M14 ko sirf “SMS bhejne wala module” nahi banana hai. Isme **Twilio messaging, templates, consent/preferences, appointment confirmations, reminders, follow-ups, missed-call SMS, delivery status, retry/idempotency, opt-out handling, scheduling, quiet hours, tenant isolation, communication logs aur AI-triggered messaging** ka proper foundation hoga.

Ek important boundary: **M14 appointment/lead business logic ko dobara implement nahi karega.** M12/M13 events consume karke communication actions karega.

# M14 — COMMUNICATION & SMS AUTOMATION ENGINE

## Complete Detailed Master Prompt for Kiro AI

```text id="m14sms"
You are a Principal SaaS Architect, Communication Platform Engineer,
Twilio Messaging Engineer, Backend Engineer, Automation Engineer,
Database Architect, AI Voice Engineer, Security Engineer,
and Multi-Tenant SaaS Reliability Engineer.

You are continuing an existing production-oriented
multi-tenant HVAC AI Employee SaaS.

============================================================
MODULE
============================================================

MODULE: M14

NAME:
COMMUNICATION & SMS AUTOMATION ENGINE

PRIMARY OBJECTIVE:

Build a production-ready communication layer that allows the
HVAC AI Employee platform to send, receive, schedule, track,
and automate customer SMS messages using the existing Twilio
integration.

M14 must support:

- SMS sending
- SMS templates
- appointment confirmation messages
- appointment reminders
- reschedule notifications
- cancellation notifications
- missed-call follow-up
- lead follow-up hooks
- customer communication preferences
- opt-out handling
- delivery status
- failed delivery handling
- retry policy
- idempotency
- message scheduling
- quiet hours
- tenant isolation
- communication logs
- AI-triggered SMS
- system-triggered SMS
- dashboard communication history
- future multi-channel extensibility

============================================================
EXISTING MODULES
============================================================

M10:
Basic AI Voice Engine

M11:
AI Tool Calling Engine

M11.2:
Tool Execution Reliability & Control

M12:
Lead Automation Engine

M13:
Appointment & Scheduling Engine

M14 builds on all of them.

DO NOT rebuild:

- Twilio credentials system
- Twilio phone configuration
- Voice engine
- Tool Registry
- Tool Executor
- Idempotency framework
- Lead system
- Appointment system
- Customer system
- Tenant middleware
- Authentication
- RBAC
- Event infrastructure
- Logger

Reuse existing architecture.

============================================================
1. M14 CORE PURPOSE
============================================================

M14 converts business events into customer communications.

Example:

Appointment confirmed
        ↓
appointment.confirmed event
        ↓
Communication Engine
        ↓
Template selection
        ↓
Customer communication preferences
        ↓
Quiet-hours check
        ↓
Idempotency
        ↓
Twilio
        ↓
SMS
        ↓
Delivery status
        ↓
Communication Log

============================================================
2. IMPORTANT ARCHITECTURAL RULE
============================================================

M14 is the communication layer.

M14 must NOT contain:

appointment scheduling logic

lead qualification logic

customer matching logic

business onboarding logic

billing logic

technician dispatch logic

Instead:

M12 owns leads.

M13 owns appointments.

M14 owns communications.

============================================================
3. COMMUNICATION ARCHITECTURE
============================================================

Expected architecture:

Business Event
      ↓
Communication Trigger
      ↓
Communication Policy
      ↓
Customer Preferences
      ↓
Template Engine
      ↓
Message Renderer
      ↓
Quiet Hours
      ↓
Idempotency
      ↓
Message Queue/Scheduler
      ↓
Twilio Messaging
      ↓
Delivery Webhook
      ↓
Communication Status
      ↓
Communication Log

============================================================
4. COMMUNICATION TYPES
============================================================

Implement communication types:

APPOINTMENT_CONFIRMATION
APPOINTMENT_REMINDER
APPOINTMENT_RESCHEDULED
APPOINTMENT_CANCELLED
MISSED_CALL
LEAD_FOLLOW_UP
CALLBACK_CONFIRMATION
CUSTOMER_REQUESTED_SMS
SYSTEM_NOTIFICATION

Keep architecture extensible.

============================================================
5. CHANNEL
============================================================

M14 initially supports:

SMS

Design the architecture so future channels can be added:

EMAIL

WHATSAPP

VOICE

PUSH

Do not implement those channels now.

============================================================
6. CHANNEL ABSTRACTION
============================================================

Create:

CommunicationProvider

Conceptual methods:

sendMessage()
getMessageStatus()
handleDeliveryEvent()
validateDestination()

Twilio should implement the SMS provider.

Do not couple the entire communication system directly
to Twilio-specific APIs.

============================================================
7. TWILIO INTEGRATION
============================================================

Reuse existing Twilio credentials and configuration.

Do NOT create duplicate credentials.

Existing architecture may contain:

TWILIO_ACCOUNT_SID
TWILIO_AUTH_TOKEN
TWILIO_PHONE_NUMBER

or messaging-service configuration.

Inspect existing M9 implementation.

Reuse it.

============================================================
8. TWILIO CREDENTIAL OWNERSHIP
============================================================

Twilio credentials belong to the platform/integration layer,
NOT to the AI model.

Never expose:

TWILIO_ACCOUNT_SID

TWILIO_AUTH_TOKEN

to:

- LLM
- browser
- customer
- frontend JavaScript

The AI only requests:

send_sms

The backend decides whether and how to send it.

============================================================
9. MULTI-TENANT PHONE CONFIGURATION
============================================================

Each business must use its own configured messaging identity
where the product architecture supports it.

Resolve:

businessId
→ messaging configuration
→ Twilio sender

Never allow:

AI-supplied fromNumber

to determine the sender.

Backend owns sender identity.

============================================================
10. COMMUNICATION MODEL
============================================================

Inspect existing Communication/Message model.

If missing, create a production-ready model.

Conceptual:

{
  _id,

  businessId,

  customerId,

  leadId,

  appointmentId,

  channel,

  direction,

  type,

  status,

  from,

  to,

  body,

  templateId,

  provider,

  providerMessageId,

  idempotencyKey,

  scheduledAt,

  sentAt,

  deliveredAt,

  failedAt,

  errorCode,

  failureReason,

  metadata,

  createdAt,

  updatedAt
}

Do not store unnecessary sensitive data.

============================================================
11. DIRECTION
============================================================

Support:

OUTBOUND

INBOUND

M14 primarily sends OUTBOUND messages.

Inbound architecture should be prepared for future support.

============================================================
12. MESSAGE STATUS
============================================================

Implement:

QUEUED
SCHEDULED
SENDING
SENT
DELIVERED
FAILED
CANCELLED
UNKNOWN

Depending on Twilio's actual delivery lifecycle.

Do not invent provider statuses.

Map provider statuses into internal statuses.

============================================================
13. PROVIDER STATUS MAPPING
============================================================

Create a normalized mapping.

Example conceptual:

Twilio accepted
→ SENT

Twilio delivered
→ DELIVERED

Twilio failed
→ FAILED

Unknown provider state
→ UNKNOWN

Inspect actual Twilio integration/API behavior before implementation.

============================================================
14. MESSAGE TYPE
============================================================

Each message must have a controlled type.

Examples:

APPOINTMENT_CONFIRMATION

APPOINTMENT_REMINDER

APPOINTMENT_RESCHEDULED

APPOINTMENT_CANCELLED

MISSED_CALL

LEAD_FOLLOW_UP

CUSTOMER_REQUESTED_SMS

SYSTEM_NOTIFICATION

Never let LLM provide arbitrary message type.

Backend determines or validates it.

============================================================
15. MESSAGE SOURCE
============================================================

Track actor/source.

Examples:

AI

SYSTEM

USER

CUSTOMER

For AI-triggered SMS:

source = AI

For appointment event:

source = SYSTEM

For dashboard manual message:

source = USER

============================================================
16. SMS TEMPLATE ENGINE
============================================================

Create:

messageTemplate.service.js

Templates should support variables.

Example:

"Hi {{customerName}}, your HVAC appointment with
{{businessName}} is confirmed for {{appointmentDate}}
at {{appointmentTime}}."

Variables should be explicitly allowed.

============================================================
17. TEMPLATE SECURITY
============================================================

Never allow templates to execute arbitrary code.

Do not support:

eval

JavaScript expressions

dynamic imports

function execution

Templates are string interpolation only.

============================================================
18. ALLOWED TEMPLATE VARIABLES
============================================================

Examples:

customerName

businessName

appointmentDate

appointmentTime

serviceType

serviceAddress

appointmentNumber

businessPhone

Do not expose:

API keys

database credentials

internal IDs

system prompts

internal notes

authorization tokens

============================================================
19. TEMPLATE RENDERING
============================================================

Input:

template

context

Output:

rendered message

Missing required variable:

TEMPLATE_RENDER_ERROR

Do not silently render:

undefined

null

[object Object]

============================================================
20. MESSAGE LENGTH
============================================================

Validate SMS length.

Do not blindly assume every message is one SMS segment.

Track:

character count

segment count if supported.

Keep automated messages concise.

============================================================
21. APPOINTMENT CONFIRMATION
============================================================

When M13 emits:

appointment.confirmed

M14 may trigger:

APPOINTMENT_CONFIRMATION

Example:

"Hi John, your appointment with ABC HVAC is confirmed
for Tuesday at 2:00 PM."

Only send if:

communication policy allows it.

Customer is eligible.

No opt-out applies.

============================================================
22. APPOINTMENT REMINDER
============================================================

Support reminders.

Example:

24 hours before appointment.

Configuration should be business-level.

Example:

appointmentReminderEnabled

reminderLeadTimeMinutes

Do not hardcode 24 hours if business configuration exists.

============================================================
23. REMINDER SCHEDULING
============================================================

When appointment is confirmed:

schedule reminder.

Example:

appointment:
Tuesday 2 PM

reminder:
Monday 2 PM

Store:

scheduledAt

The scheduler later sends the message.

============================================================
24. REMINDER CANCELLATION
============================================================

If appointment is:

CANCELLED

or

RESCHEDULED

old reminder must not send.

Before sending:

re-check appointment state.

Do not rely only on old scheduler data.

============================================================
25. RESCHEDULED APPOINTMENT
============================================================

When:

appointment.rescheduled

M14 may send:

APPOINTMENT_RESCHEDULED

Then:

cancel/invalidate old reminder

schedule new reminder

according to communication policy.

============================================================
26. CANCELLED APPOINTMENT
============================================================

When:

appointment.cancelled

M14 may send:

APPOINTMENT_CANCELLED

Do not send reminder afterward.

============================================================
27. MISSED CALL SMS
============================================================

Support:

MISSED_CALL

Example:

"Hi, this is ABC HVAC. We missed your call. Reply here or
call us back and we'll be happy to help."

This must only trigger when the call is actually classified
as missed/unanswered according to the existing call system.

Do not send SMS for every completed call.

============================================================
28. MISSED CALL IDEMPOTENCY
============================================================

Same missed call event must not send multiple SMS messages.

Use:

businessId
+
callSid
+
messageType

or equivalent idempotency key.

Reuse M11.2 idempotency architecture.

============================================================
29. CUSTOMER REQUESTED SMS
============================================================

During voice call:

Customer:
"Can you text me that?"

AI may call:

send_sms

through M11/M11.2.

M14 becomes the underlying communication service.

============================================================
30. AI SMS SECURITY
============================================================

The AI must NOT control:

from number

businessId

provider credentials

message type

arbitrary customer IDs

permissions

The AI provides only:

approved destination/context

message content

The backend validates everything.

============================================================
31. CUSTOMER DESTINATION
============================================================

Prefer verified/current caller phone number.

Do not allow arbitrary SMS destinations.

If destination differs from caller:

require appropriate authorization/business policy.

============================================================
32. SMS CONSENT / OPT-OUT
============================================================

Implement communication preference state.

Possible:

SMS_ALLOWED

SMS_OPTED_OUT

UNKNOWN

Do not send promotional/automated messages to opted-out
customers where policy prohibits it.

============================================================
33. STOP / OPT-OUT HANDLING
============================================================

Support common opt-out concepts.

Examples:

STOP

UNSUBSCRIBE

CANCEL

QUIT

If inbound messaging infrastructure is available:

detect opt-out.

Set:

SMS_OPTED_OUT

Do not send future eligible automated SMS.

============================================================
34. OPT-IN
============================================================

Do not automatically reverse opt-out because customer sends
a normal message.

If re-subscription is supported:

require explicit opt-in according to the communication policy.

============================================================
35. TRANSACTIONAL VS MARKETING
============================================================

Separate:

TRANSACTIONAL

MARKETING

M14 focuses primarily on transactional/service communications.

Examples:

appointment confirmation

appointment reminder

appointment cancellation

callback confirmation

Do not build a marketing campaign system.

============================================================
36. MESSAGE CATEGORY
============================================================

Store:

category:

TRANSACTIONAL

or

MARKETING

Default M14 automated appointment messages:

TRANSACTIONAL

============================================================
37. COMMUNICATION PREFERENCES
============================================================

Extend Customer preferences if existing model supports it.

Conceptual:

communicationPreferences: {
  sms: {
    enabled,
    transactionalEnabled,
    marketingEnabled
  }
}

Do not duplicate preference systems.

============================================================
38. BUSINESS COMMUNICATION POLICY
============================================================

Business may configure:

SMS enabled

appointment confirmations

appointment reminders

missed call SMS

follow-up SMS

quiet hours

reminder timing

Do not build a complete marketing automation UI.

============================================================
39. QUIET HOURS
============================================================

Implement quiet hours.

Example:

21:00–08:00

Automated non-urgent messages should not be sent during
quiet hours where policy requires.

Instead:

schedule for next permitted time.

============================================================
40. TIMEZONE
============================================================

Quiet hours must use:

business/customer communication timezone according to
product policy.

Do not use server timezone.

For appointment messages:

use business timezone unless customer-specific timezone
is explicitly supported.

============================================================
41. URGENT MESSAGES
============================================================

Define policy for urgent transactional messages.

Do not assume every message bypasses quiet hours.

Keep policy configurable.

============================================================
42. MESSAGE SCHEDULER
============================================================

Create:

communicationScheduler.service.js

Responsibilities:

- schedule message
- find due messages
- execute message
- mark status
- retry eligible failures
- prevent duplicates

Do not build an uncontrolled infinite polling loop.

============================================================
43. SCHEDULER ARCHITECTURE
============================================================

If existing job/queue system exists:

reuse it.

Preferred future-compatible abstraction:

JobQueue

CommunicationJob

Do not introduce Redis/BullMQ/etc. if an existing scheduler
already solves the problem.

If no queue exists:

implement a minimal reliable scheduler appropriate to the
existing deployment architecture.

============================================================
44. SCHEDULER REQUIREMENTS
============================================================

Scheduler must support:

scheduledAt

status

attemptCount

nextAttemptAt

lastError

idempotencyKey

============================================================
45. MESSAGE QUEUE STATES
============================================================

Example:

SCHEDULED
 ↓
READY
 ↓
SENDING
 ↓
SENT
 ↓
DELIVERED

Failure:

SENDING
 ↓
FAILED

Retryable failure:

FAILED
 ↓
SCHEDULED
 ↓
SENDING

============================================================
46. RETRY POLICY
============================================================

Reuse M11.2 retry principles.

Retry only transient failures.

Do not retry:

invalid phone

opt-out

invalid template

authorization failure

permanent provider rejection

Retry:

temporary provider outage

network timeout

temporary service unavailable

============================================================
47. MAX RETRIES
============================================================

Use configuration.

Example:

SMS_MAX_RETRIES=2

Do not retry indefinitely.

============================================================
48. BACKOFF
============================================================

Use controlled exponential backoff.

Do not create long delays for urgent transactional messages
unless required.

============================================================
49. UNKNOWN PROVIDER RESULT
============================================================

Important:

Twilio request times out.

We don't know if message was accepted.

Do NOT blindly send another SMS.

Use:

EXTERNAL_RESULT_UNKNOWN

and reconciliation/status webhook where possible.

============================================================
50. TWILIO MESSAGE ID
============================================================

Store provider message identifier:

providerMessageId

when returned.

This is used to correlate delivery status.

============================================================
51. DELIVERY STATUS WEBHOOK
============================================================

Create/extend Twilio messaging status callback.

Conceptual:

POST /webhooks/twilio/messaging/status

Map provider status to internal status.

Verify webhook authenticity using the existing secure
Twilio webhook validation mechanism.

============================================================
52. WEBHOOK SECURITY
============================================================

Do not trust arbitrary requests to status endpoint.

Validate:

Twilio signature

according to existing Twilio webhook architecture.

Do not disable signature validation in production.

============================================================
53. DELIVERY UPDATE
============================================================

When Twilio reports:

delivered

update message.

When:

failed

update message.

Do not allow webhook to modify another tenant's record.

Resolve by:

providerMessageId

then verify business association.

============================================================
54. INBOUND SMS PREPARATION
============================================================

M14 may create:

POST /webhooks/twilio/messaging/inbound

if existing architecture supports it.

But do not build a complete two-way SMS chatbot.

Prepare for future inbound communication.

============================================================
55. INBOUND SMS SECURITY
============================================================

Verify:

Twilio signature

phone number

business mapping

message normalization.

============================================================
56. INBOUND MESSAGE STORAGE
============================================================

If inbound messages are stored:

use:

direction = INBOUND

source = CUSTOMER

Do not store unnecessary metadata.

============================================================
57. COMMUNICATION LOG
============================================================

Create/extend:

CommunicationLog

Each message should capture:

businessId

customerId

leadId

appointmentId

channel

type

direction

status

source

provider

providerMessageId

createdAt

sentAt

deliveredAt

failedAt

============================================================
58. COMMUNICATION LOG SECURITY
============================================================

Do not expose:

Twilio credentials

internal errors

authorization headers

system prompts

private internal metadata

============================================================
59. MESSAGE BODY PRIVACY
============================================================

Message body is customer communication data.

Use appropriate access control.

Do not put message body into general application logs.

============================================================
60. LOGGING
============================================================

Structured log:

COMMUNICATION_CREATED

COMMUNICATION_SCHEDULED

COMMUNICATION_SEND_STARTED

COMMUNICATION_SENT

COMMUNICATION_DELIVERED

COMMUNICATION_FAILED

COMMUNICATION_RETRY

COMMUNICATION_CANCELLED

COMMUNICATION_OPT_OUT

Do not log full message body by default.

============================================================
61. AUDIT
============================================================

For manual dashboard sends:

record:

actorType = USER

For AI:

actorType = AI

For automation:

actorType = SYSTEM

============================================================
62. TENANT ISOLATION
============================================================

Every communication record:

businessId

Every query:

businessId scoped.

Never allow:

businessId from frontend

to override authenticated tenant.

============================================================
63. CUSTOMER OWNERSHIP
============================================================

Before sending:

verify:

customer.businessId === context.businessId

Do not send to another tenant's customer.

============================================================
64. LEAD OWNERSHIP
============================================================

If message references lead:

verify:

lead.businessId === context.businessId

and:

lead.customerId === customerId

where applicable.

============================================================
65. APPOINTMENT OWNERSHIP
============================================================

If message references appointment:

verify:

appointment.businessId === context.businessId

and:

appointment.customerId === customerId

============================================================
66. MESSAGE CONTEXT
============================================================

Create:

CommunicationContext

Example:

{
  businessId,
  customerId,
  leadId,
  appointmentId,
  channel,
  messageType,
  source,
  actorType,
  timezone
}

Backend owns:

businessId

actor permissions

source validation.

============================================================
67. MESSAGE CREATION FLOW
============================================================

Example:

appointment.confirmed

↓

communication trigger

↓

load appointment

↓

load customer

↓

validate business

↓

check communication preferences

↓

select template

↓

render template

↓

check quiet hours

↓

generate idempotency key

↓

schedule/send

↓

Twilio

↓

store providerMessageId

↓

delivery webhook

↓

update status

============================================================
68. APPOINTMENT CONFIRMATION IDEMPOTENCY
============================================================

Same appointment.confirmed event must not send duplicate
confirmation SMS.

Key example:

businessId
+
appointmentId
+
APPOINTMENT_CONFIRMATION

============================================================
69. REMINDER IDEMPOTENCY
============================================================

Same reminder job must not send twice.

Key:

businessId
+
appointmentId
+
reminderType
+
scheduledTime

============================================================
70. RESCHEDULE IDEMPOTENCY
============================================================

Same reschedule event:

must not send multiple notifications.

Use:

businessId
+
appointmentId
+
newScheduledStart
+
APPOINTMENT_RESCHEDULED

============================================================
71. CANCELLATION IDEMPOTENCY
============================================================

Same cancellation event:

must not send duplicate SMS.

============================================================
72. MISSED CALL IDEMPOTENCY
============================================================

Use:

businessId
+
callSid
+
MISSED_CALL

============================================================
73. MANUAL SMS IDEMPOTENCY
============================================================

Dashboard user manually sending an SMS should have a safe
idempotency strategy where appropriate.

Do not accidentally send multiple messages from request retries.

============================================================
74. MESSAGE CANCELLATION
============================================================

Scheduled message may be cancelled if:

appointment cancelled

appointment rescheduled

customer opts out

business disables communication

message no longer relevant

Do not cancel already-delivered messages.

============================================================
75. REMINDER REVALIDATION
============================================================

Before sending reminder:

re-fetch or verify current appointment state.

Example:

Reminder scheduled Monday.

Appointment cancelled Sunday.

Monday scheduler runs.

It must NOT send reminder.

============================================================
76. APPOINTMENT TIME REVALIDATION
============================================================

Before reminder:

use current appointment time.

Do not trust stale scheduledAt context.

============================================================
77. TEMPLATE VERSIONING
============================================================

Templates may change.

Store:

templateId

templateVersion

where practical.

This helps audit which message version was sent.

============================================================
78. TEMPLATE MANAGEMENT
============================================================

Business admins may eventually customize templates.

M14 may create basic template CRUD if needed.

Do not build an advanced drag-and-drop template builder.

============================================================
79. TEMPLATE VARIABLES UI
============================================================

If template UI exists:

show supported variables.

Example:

{{customerName}}

{{businessName}}

{{appointmentDate}}

{{appointmentTime}}

Do not allow arbitrary code.

============================================================
80. DEFAULT TEMPLATES
============================================================

Provide sensible default templates for:

Appointment Confirmation

Appointment Reminder

Appointment Rescheduled

Appointment Cancelled

Missed Call

Callback Confirmation

Keep templates concise and professional.

============================================================
81. BUSINESS TEMPLATE OVERRIDE
============================================================

Business may customize allowed templates.

But:

system must validate rendered result.

Do not allow:

empty message

unsupported variables

dangerous markup/code

============================================================
82. MESSAGE PREVIEW
============================================================

Dashboard template editor may show:

preview

with sample values.

Do not send actual SMS during preview.

============================================================
83. DASHBOARD COMMUNICATION PAGE
============================================================

Create or extend:

/dashboard/communications

Show:

customer

message type

channel

status

direction

date

source

appointment/lead reference

============================================================
84. COMMUNICATION DETAIL
============================================================

Show:

recipient

type

status

provider

sent time

delivered time

failure reason where appropriate

template

related appointment/lead

Do not expose secrets.

============================================================
85. MESSAGE BODY ACCESS
============================================================

Authorized business users may view message body according
to RBAC.

Do not expose message content to unauthorized users.

============================================================
86. FILTERS
============================================================

Support:

status

type

channel

direction

source

date range

customer

appointment

lead

============================================================
87. PAGINATION
============================================================

Communication list must be paginated.

Do not load thousands of messages.

============================================================
88. SEARCH
============================================================

Search may include:

customer name

phone

provider message ID

appointment number

where appropriate.

Do not expose arbitrary DB search operators.

============================================================
89. MOBILE UI
============================================================

Communication page must work on mobile.

Use responsive cards/list.

============================================================
90. EMPTY STATE
============================================================

If no messages:

"No customer communications yet."

============================================================
91. ERROR STATE
============================================================

"Unable to load communications."

Provide retry.

Never expose raw provider errors.

============================================================
92. LOADING STATE
============================================================

Use existing skeleton/loading system.

============================================================
93. MANUAL SEND SMS UI
============================================================

Authorized users may send SMS if business policy allows.

Flow:

Select customer
 ↓
Write message
 ↓
Preview
 ↓
Check preference
 ↓
Send
 ↓
Status

Backend performs all validation.

============================================================
94. MANUAL SMS SECURITY
============================================================

Frontend must NOT send directly to Twilio.

Correct:

Frontend
 ↓
Backend
 ↓
Communication Service
 ↓
Twilio

============================================================
95. AI SEND SMS
============================================================

AI:

send_sms

↓

M11.2 Tool Executor

↓

M14 Communication Service

↓

policy

↓

preference

↓

Twilio

This prevents AI from bypassing M14.

============================================================
96. AI MESSAGE CONTENT
============================================================

AI-generated SMS must pass:

length validation

policy

destination validation

opt-out

business context

Do not allow AI to send arbitrary promotional messages.

============================================================
97. AI SMS TYPE
============================================================

AI must specify/use an allowed purpose.

Examples:

CUSTOMER_REQUESTED_SMS

CALLBACK_CONFIRMATION

APPOINTMENT_INFORMATION

Do not permit:

MARKETING

unless explicitly enabled by future module.

============================================================
98. MESSAGE RATE LIMIT
============================================================

Protect against runaway AI messaging.

Example:

AI_SMS_MAX_PER_SESSION=3

AI_SMS_MAX_PER_CUSTOMER_PER_HOUR=5

Use configuration.

Do not allow unlimited AI SMS.

============================================================
99. AUTOMATION RATE LIMIT
============================================================

Protect against accidental automation storms.

Example:

maximum automated messages per customer/day

according to business policy.

Do not overengineer.

============================================================
100. DUPLICATE CONTENT PROTECTION
============================================================

Optional protection against sending identical messages repeatedly
within a short period.

Example:

same customer

same message type

same rendered content

short time window.

Do not block legitimate repeated appointment reminders.

============================================================
101. COMMUNICATION PREFERENCES
============================================================

Customer preference rules must be evaluated before every
automated message.

Do not assume preference from previous message.

============================================================
102. BUSINESS DISABLE
============================================================

If business disables SMS:

no new outbound SMS should be sent.

Already delivered messages remain historical.

Scheduled messages should be cancelled/disabled.

============================================================
103. TWILIO CONFIGURATION FAILURE
============================================================

If business messaging configuration is missing:

do not attempt send.

Return:

COMMUNICATION_NOT_CONFIGURED

AI gets safe response.

Dashboard gets actionable error.

============================================================
104. INVALID PHONE
============================================================

Normalize and validate.

If invalid:

INVALID_DESTINATION

Do not send.

============================================================
105. US PHONE NUMBER SUPPORT
============================================================

Primary target is US HVAC businesses.

Use robust E.164 normalization.

Example:

+1XXXXXXXXXX

Do not assume all numbers are US if existing platform supports
international expansion.

============================================================
106. MESSAGE FAILURE
============================================================

If Twilio rejects:

mark FAILED.

Store safe normalized error.

Do not expose raw provider response to customer.

============================================================
107. DELIVERY FAILURE
============================================================

If Twilio later reports failure:

update communication status.

Do not automatically resend unless retry policy explicitly
allows it.

============================================================
108. DELIVERY WEBHOOK IDEMPOTENCY
============================================================

Twilio may send duplicate webhook events.

Handle duplicate status events safely.

Do not create duplicate communication records.

============================================================
109. STATUS TRANSITION RULES
============================================================

Do not allow:

DELIVERED → SENDING

SENT → QUEUED

FAILED → DELIVERED

unless provider reconciliation explicitly justifies it.

Create normalized state transitions.

============================================================
110. COMMUNICATION STATE MACHINE
============================================================

Implement:

QUEUED
→ SENDING
→ SENT
→ DELIVERED

Failure:

SENDING
→ FAILED

Scheduled:

SCHEDULED
→ QUEUED

Cancellation:

SCHEDULED
→ CANCELLED

Do not allow arbitrary transitions.

============================================================
111. EVENT CONSUMPTION
============================================================

M14 consumes events from:

M12:
lead.created
lead.qualified
lead.follow_up_required

M13:
appointment.confirmed
appointment.rescheduled
appointment.cancelled

M10/M11:
missed call event where supported.

============================================================
112. EVENT FILTERING
============================================================

Not every event should create a message.

Use:

CommunicationTriggerPolicy

Example:

appointment.confirmed
→ confirmation SMS

lead.created
→ no automatic SMS by default

lead.follow_up_required
→ communication event/hook

============================================================
113. COMMUNICATION TRIGGER POLICY
============================================================

Create:

communicationPolicy.service.js

Responsibilities:

- determine whether message should be sent
- determine message type
- determine template
- check preference
- check quiet hours
- check business configuration

Do not put actual Twilio API logic here.

============================================================
114. TEMPLATE SELECTION
============================================================

Policy:

event

+
business configuration

+
communication type

→ template

If custom template missing:

use safe default.

============================================================
115. QUIET HOURS POLICY
============================================================

If message is not allowed now:

schedule next allowed time.

Do not silently drop the message unless policy explicitly says so.

============================================================
116. CUSTOMER TIMEZONE
============================================================

If customer timezone is unknown:

use business timezone.

Do not infer customer timezone from phone area code.

============================================================
117. MESSAGE SCHEDULER
============================================================

Scheduler should safely process:

due messages

failed retryable messages

cancelled messages

expired messages.

============================================================
118. EXPIRED MESSAGE
============================================================

If message is no longer relevant:

mark:

CANCELLED

or

EXPIRED

depending on architecture.

Example:

appointment reminder for appointment already completed.

Do not send.

============================================================
119. COMMUNICATION EXPIRY
============================================================

Support optional:

expiresAt

For time-sensitive messages.

Example:

appointment reminder.

============================================================
120. APPOINTMENT CONFIRMATION EXPIRY
============================================================

Confirmation SMS is normally sent immediately.

No need for long expiration.

============================================================
121. LEAD FOLLOW-UP
============================================================

M14 should expose communication capabilities to future
follow-up automation.

M12 may emit:

lead.follow_up_required

M14 may create:

communication job

But only if business policy allows.

Do not build a full lead follow-up sequence engine yet.

============================================================
122. FOLLOW-UP SAFETY
============================================================

Do not send repeated follow-ups indefinitely.

Use:

attempt count

nextFollowUpAt

communication policy

customer preferences

============================================================
123. NO MARKETING CAMPAIGN
============================================================

Do NOT implement:

mass SMS

marketing campaigns

bulk messaging

drip campaigns

marketing segmentation

Those belong to future modules.

============================================================
124. BULK SMS SAFETY
============================================================

There must be no generic endpoint:

POST /send-sms-to-all

Do not expose bulk messaging.

============================================================
125. MESSAGE QUEUE SAFETY
============================================================

Do not allow frontend users to enqueue arbitrary communication
jobs for other businesses.

Every job must be tenant-scoped.

============================================================
126. API ENDPOINTS
============================================================

Use existing API conventions.

Conceptual:

GET /api/communications

GET /api/communications/:id

POST /api/communications/sms

POST /api/communications/:id/cancel

GET /api/customers/:id/communications

GET /api/appointments/:id/communications

GET /api/leads/:id/communications

Do not duplicate existing routes.

============================================================
127. TEMPLATE API
============================================================

If implemented:

GET /api/message-templates

GET /api/message-templates/:id

POST /api/message-templates

PATCH /api/message-templates/:id

All tenant scoped.

============================================================
128. SCHEDULING API
============================================================

Internal scheduler should not be exposed as arbitrary public
job creation API.

Do not allow:

POST /api/jobs

with arbitrary executable action.

============================================================
129. COMMUNICATION API VALIDATION
============================================================

Validate:

customerId

phone

message

messageType

templateId

scheduledAt

channel

============================================================
130. SOURCE VALIDATION
============================================================

Frontend cannot claim:

source = SYSTEM

Frontend user action must be:

USER

AI tool must be:

AI

Automation event:

SYSTEM

Backend derives source.

============================================================
131. RBAC
============================================================

Authorized roles may:

view communications

send manual SMS

manage templates

configure SMS settings

Do not allow every role to perform all actions.

Use existing RBAC.

============================================================
132. AI PERMISSIONS
============================================================

AI may:

send approved customer-requested SMS

send approved transactional communication

AI may NOT:

edit communication policy

disable opt-out

change Twilio sender

send bulk messages

send marketing campaigns

============================================================
133. ADMIN SETTINGS
============================================================

Business communication settings may include:

SMS enabled

appointment confirmation enabled

appointment reminder enabled

missed-call SMS enabled

quiet hours

reminder timing

template selection

Do not create platform-wide settings UI inside business settings.

============================================================
134. TWILIO SENDER SELECTION
============================================================

Backend determines:

business → messaging identity

Possible:

Twilio phone number

Messaging Service SID

Do not let AI/frontend choose arbitrary sender.

============================================================
135. MULTI-TENANT TWILIO
============================================================

If architecture uses platform-owned Twilio credentials:

business mapping determines sender.

If architecture later supports business-owned Twilio accounts:

provider configuration abstraction should allow it.

Do not expose credentials to AI.

============================================================
136. PROVIDER OWNERSHIP MODEL
============================================================

Prepare architecture for:

PLATFORM_MANAGED

BUSINESS_MANAGED

Do not implement business-managed credential UI unless already
part of previous modules.

============================================================
137. PROVIDER FAILURE
============================================================

If Twilio unavailable:

mark failure/unknown appropriately.

Do not crash voice engine.

M10 voice call should continue if possible.

============================================================
138. VOICE INTEGRATION
============================================================

If customer asks:

"Text me the appointment details."

AI:

send_sms

M14:

validate

send

return result

AI:

"Done, I've sent that information by text."

ONLY say this after actual success.

============================================================
139. VOICE FAILURE
============================================================

If SMS fails:

AI must NOT say:

"I sent it."

Instead:

"I wasn't able to send that text right now."

============================================================
140. COMMUNICATION RESULT CONTRACT
============================================================

Success:

{
  success: true,
  data: {
    communicationId,
    status,
    providerMessageId
  }
}

Failure:

{
  success: false,
  error: {
    code,
    message,
    retryable
  }
}

============================================================
141. MESSAGE AUDIT
============================================================

Every outbound message should be traceable to:

event

AI action

user action

or system trigger.

Example:

triggerType:
APPOINTMENT_CONFIRMED

source:
SYSTEM

============================================================
142. CORRELATION
============================================================

Use:

businessId

sessionId

callId

communicationId

providerMessageId

eventId

where available.

This enables:

Call
→ AI
→ Tool
→ Appointment
→ SMS

traceability.

============================================================
143. DATABASE INDEXES
============================================================

Inspect actual queries.

Potential indexes:

businessId + createdAt

businessId + customerId + createdAt

businessId + appointmentId

businessId + leadId

businessId + status + scheduledAt

providerMessageId

idempotencyKey

Do not blindly add every index.

============================================================
144. UNIQUE INDEX STRATEGY
============================================================

If using unique idempotency keys:

scope appropriately.

Do not create global uniqueness that causes cross-tenant collisions.

============================================================
145. RETENTION
============================================================

Do not implement aggressive deletion.

Communication history may be important for support/audit.

If retention policy exists:

reuse it.

Do not delete customer communication automatically without
documented policy.

============================================================
146. PRIVACY
============================================================

Treat:

phone numbers

message bodies

addresses

appointment details

as customer/business data.

Protect through existing access controls.

============================================================
147. NO CHAIN OF THOUGHT
============================================================

Never store:

LLM hidden reasoning

internal system prompt

model chain-of-thought.

Store only:

structured message metadata.

============================================================
148. TESTING — TEMPLATE
============================================================

Test:

valid variables

missing variable

unsupported variable

empty template

oversized message

special characters

============================================================
149. TESTING — PREFERENCES
============================================================

Test:

SMS enabled

SMS disabled

opted out

transactional allowed

marketing disabled

============================================================
150. TESTING — QUIET HOURS
============================================================

Test:

inside allowed time

inside quiet hours

boundary time

overnight quiet hours

timezone differences

============================================================
151. TESTING — APPOINTMENT CONFIRMATION
============================================================

Event:

appointment.confirmed

Expected:

one confirmation message.

Duplicate event:

still one message.

============================================================
152. TESTING — REMINDER
============================================================

Appointment:

tomorrow 2 PM.

Reminder:

configured lead time.

Expected:

message scheduled correctly.

============================================================
153. TESTING — CANCELLED REMINDER
============================================================

Schedule reminder.

Cancel appointment.

Scheduler runs.

Expected:

no reminder sent.

============================================================
154. TESTING — RESCHEDULE
============================================================

Appointment rescheduled.

Expected:

old reminder invalidated.

new reminder scheduled.

One reschedule notification.

============================================================
155. TESTING — MISSED CALL
============================================================

Missed call event.

Expected:

one SMS.

Duplicate event:

no duplicate SMS.

============================================================
156. TESTING — AI SMS
============================================================

AI requests SMS.

Expected:

M11.2 executor
→ M14
→ validation
→ Twilio.

============================================================
157. TESTING — AI UNAUTHORIZED SMS
============================================================

AI attempts:

marketing message

bulk message

arbitrary destination

Expected:

rejected.

============================================================
158. TESTING — TWILIO FAILURE
============================================================

Simulate:

Twilio failure.

Expected:

FAILED.

AI receives safe error.

No false success.

============================================================
159. TESTING — TWILIO TIMEOUT
============================================================

Simulate timeout.

Expected:

EXTERNAL_RESULT_UNKNOWN

or appropriate state.

Do not blindly send duplicate message.

============================================================
160. TESTING — DELIVERY WEBHOOK
============================================================

Send:

delivered

failed

duplicate webhook

Expected:

correct normalized state.

No duplicate communication record.

============================================================
161. TESTING — TENANT ISOLATION
============================================================

Business A:

Customer A

Communication A

Business B:

Customer B

Communication B

Business A cannot:

read B

send to B

update B

cancel B

============================================================
162. TESTING — CUSTOMER OWNERSHIP
============================================================

Business A user attempts:

send SMS to Business B customer.

Expected:

DENIED.

============================================================
163. TESTING — APPOINTMENT OWNERSHIP
============================================================

Business A communication references Business B appointment.

Expected:

DENIED.

============================================================
164. TESTING — IDEMPOTENCY
============================================================

Same event twice.

Expected:

one message.

Same scheduler job twice.

Expected:

one message.

Same AI tool call twice.

Expected:

one message.

============================================================
165. TESTING — RETRIES
============================================================

Temporary Twilio failure.

Expected:

retry according to policy.

Permanent failure.

Expected:

no endless retry.

============================================================
166. TESTING — RATE LIMIT
============================================================

AI sends multiple SMS requests.

Expected:

session/customer limits enforced.

============================================================
167. TESTING — OPT OUT
============================================================

Customer opts out.

Then appointment reminder triggers.

Expected:

blocked according to policy.

============================================================
168. TESTING — BUSINESS DISABLE
============================================================

Business disables SMS.

Existing scheduled message:

cancel/prevent send according to policy.

New message:

rejected.

============================================================
169. TESTING — REGRESSION
============================================================

After M14:

M10 voice works.

M11 tools work.

M11.2 reliability works.

M12 lead works.

M13 appointment works.

M14 communication works.

============================================================
170. PERFORMANCE
============================================================

Do not block voice calls waiting unnecessarily for SMS delivery.

AI send_sms tool may wait only for provider acceptance/result
required by the tool contract.

Do not wait for final delivery status unless explicitly needed.

============================================================
171. ASYNC DELIVERY
============================================================

Sending SMS and delivery confirmation are separate.

Example:

send request:
→ SENT

later:

Twilio webhook:
→ DELIVERED

Do not block the voice conversation until DELIVERED.

============================================================
172. MESSAGE SCHEDULER PERFORMANCE
============================================================

Process messages in bounded batches.

Do not load all scheduled messages into memory.

Use indexed queries.

============================================================
173. FAILURE ISOLATION
============================================================

Communication failure must not crash:

voice engine

lead engine

appointment engine

business dashboard.

Use isolated error handling.

============================================================
174. CLEAN ARCHITECTURE
============================================================

Recommended structure:

src/
  communication/
    communication.service.js
    communication.policy.js
    communication.context.js

    providers/
      communication.provider.js
      twilioSms.provider.js

    templates/
      template.service.js
      template.renderer.js

    scheduler/
      communication.scheduler.js

    delivery/
      deliveryStatus.service.js

    preferences/
      communicationPreference.service.js

    audit/
      communicationAudit.service.js

    idempotency/
      communicationIdempotency.service.js

Adapt to existing architecture.

Do not duplicate equivalent modules.

============================================================
175. SERVICE SEPARATION
============================================================

CommunicationService:

orchestrates communication.

TemplateService:

renders templates.

PolicyService:

decides whether communication is allowed.

Provider:

talks to Twilio.

Scheduler:

handles timing.

DeliveryService:

handles provider status.

PreferenceService:

handles opt-in/opt-out.

AuditService:

records communication history.

============================================================
176. NO BUSINESS LOGIC IN TWILIO PROVIDER
============================================================

Twilio provider should only handle:

send

status

provider mapping

It should not decide:

appointment reminder

lead follow-up

customer qualification.

============================================================
177. NO AI LOGIC IN TWILIO PROVIDER
============================================================

AI decisions happen above communication service.

Twilio provider remains generic.

============================================================
178. NO DIRECT TWILIO FROM CONTROLLERS
============================================================

Controllers should call:

CommunicationService

not:

Twilio SDK directly.

============================================================
179. NO DIRECT TWILIO FROM AI TOOL
============================================================

AI tool should call:

CommunicationService

not Twilio SDK.

============================================================
180. API DOCUMENTATION
============================================================

Document:

communication APIs

template APIs

webhooks

status lifecycle

preference behavior

quiet hours

retry policy

idempotency

============================================================
181. README
============================================================

Update README with:

M14 architecture

Twilio SMS setup

message lifecycle

templates

communication preferences

opt-out

quiet hours

scheduler

delivery webhooks

retry behavior

idempotency

testing

troubleshooting

============================================================
182. ENVIRONMENT VARIABLES
============================================================

Inspect existing configuration first.

Possible:

SMS_ENABLED=true

SMS_MAX_RETRIES=2

AI_SMS_MAX_PER_SESSION=3

AI_SMS_MAX_PER_CUSTOMER_PER_HOUR=5

COMMUNICATION_DEFAULT_QUIET_START=21:00

COMMUNICATION_DEFAULT_QUIET_END=08:00

Do not duplicate existing variables.

Do not add unnecessary settings.

============================================================
183. TWILIO WEBHOOK CONFIGURATION
============================================================

Document:

Messaging status callback URL

Inbound messaging webhook if implemented

Required signature validation

============================================================
184. SECURITY CHECKLIST
============================================================

[ ] Twilio credentials server-side only
[ ] tenant isolation
[ ] authenticated APIs
[ ] RBAC
[ ] webhook signature validation
[ ] destination validation
[ ] template validation
[ ] no arbitrary sender
[ ] AI cannot bypass policy
[ ] AI cannot bypass opt-out
[ ] no bulk messaging
[ ] rate limits
[ ] idempotency
[ ] retry limits
[ ] PII-safe logs
[ ] no raw provider secrets
[ ] no chain-of-thought

============================================================
185. FINAL ARCHITECTURE
============================================================

M14 should result in:

M12/M13 EVENT
       │
       ▼
COMMUNICATION POLICY
       │
       ├── Business enabled?
       ├── Customer allowed?
       ├── Correct message type?
       ├── Quiet hours?
       └── Duplicate?
       │
       ▼
TEMPLATE ENGINE
       │
       ▼
MESSAGE RENDERER
       │
       ▼
IDEMPOTENCY
       │
       ▼
SCHEDULER
       │
       ▼
COMMUNICATION SERVICE
       │
       ▼
TWILIO SMS PROVIDER
       │
       ▼
CUSTOMER PHONE
       │
       ▼
TWILIO STATUS WEBHOOK
       │
       ▼
DELIVERY SERVICE
       │
       ▼
COMMUNICATION LOG
```

============================================================
186. FINAL ACCEPTANCE CRITERIA
==============================

M14 is complete only when:

[ ] Communication architecture exists
[ ] SMS provider abstraction exists
[ ] Existing Twilio integration reused
[ ] Twilio credentials remain server-side
[ ] Multi-tenant sender resolution works
[ ] Communication model exists/extended
[ ] Message lifecycle exists
[ ] Communication state machine exists
[ ] Template engine exists
[ ] Template validation exists
[ ] Default templates exist
[ ] Appointment confirmation works
[ ] Appointment reminder works
[ ] Reschedule notification works
[ ] Cancellation notification works
[ ] Missed-call SMS works
[ ] AI requested SMS works
[ ] Communication preferences work
[ ] Opt-out handling works
[ ] Quiet hours work
[ ] Scheduler works
[ ] Reminder revalidation works
[ ] Retry policy works
[ ] Timeout handling works
[ ] Unknown provider result handled
[ ] Delivery webhook works
[ ] Twilio signature validation works
[ ] Communication logs work
[ ] Idempotency works
[ ] Rate limiting works
[ ] Tenant isolation works
[ ] Customer ownership works
[ ] Lead ownership works
[ ] Appointment ownership works
[ ] Dashboard communication page exists
[ ] Communication detail exists
[ ] Manual SMS UI exists if appropriate
[ ] Template management exists if appropriate
[ ] Mobile UI works
[ ] Loading states work
[ ] Empty states work
[ ] Error states work
[ ] API tests pass
[ ] Template tests pass
[ ] Preference tests pass
[ ] Quiet-hours tests pass
[ ] Reminder tests pass
[ ] Idempotency tests pass
[ ] Twilio failure tests pass
[ ] Delivery webhook tests pass
[ ] Tenant security tests pass
[ ] M10 regression passes
[ ] M11 regression passes
[ ] M11.2 regression passes
[ ] M12 regression passes
[ ] M13 regression passes
[ ] README updated
[ ] .env.example updated
[ ] No M15+ functionality was prematurely implemented

============================================================
187. FINAL IMPLEMENTATION REPORT
================================

After implementation provide a detailed report:

1. Existing M10-M13 communication-related architecture
2. Existing Twilio implementation
3. Files created
4. Files modified
5. Files reused
6. Communication architecture
7. Communication model
8. Message lifecycle
9. State machine
10. Twilio provider
11. Provider abstraction
12. Sender resolution
13. Template engine
14. Default templates
15. Template variables
16. Communication policy
17. Customer preferences
18. Opt-out handling
19. Quiet hours
20. Scheduler
21. Reminder system
22. Appointment confirmation
23. Reschedule notification
24. Cancellation notification
25. Missed-call SMS
26. AI SMS integration
27. Idempotency
28. Retry policy
29. Timeout behavior
30. Unknown provider result handling
31. Delivery webhooks
32. Twilio signature validation
33. Communication audit
34. Tenant isolation
35. RBAC
36. Rate limiting
37. API endpoints
38. Dashboard UI
39. Tests
40. Security tests
41. Voice tests
42. Environment changes
43. Migration requirements
44. Known limitations
45. M10 regression
46. M11 regression
47. M11.2 regression
48. M12 regression
49. M13 regression
50. Confirmation that M15+ was NOT prematurely implemented

============================================================
START NOW
=========

FIRST:

Inspect M1-M13.

Especially inspect:

* M9 Twilio configuration
* M10 voice engine
* M11 send_sms tool
* M11.2 reliability layer
* M12 Lead events
* M13 Appointment events
* existing Customer communication preferences
* existing job/queue system

THEN:

1. Create implementation plan.
2. Map M14 onto existing architecture.
3. Implement communication provider abstraction.
4. Implement communication service.
5. Implement templates.
6. Implement policies/preferences.
7. Implement scheduler.
8. Implement delivery status.
9. Implement idempotency.
10. Implement appointment communication triggers.
11. Implement AI SMS integration.
12. Implement dashboard.
13. Run tests.
14. Run regression tests.
15. Fix failures.
16. Update documentation.
17. Provide final implementation report.

Do not claim completion unless the acceptance criteria are actually satisfied.

Never fake an SMS send.

Never claim an SMS was delivered unless provider status confirms it.

Never expose Twilio credentials.

Never bypass customer opt-out.

Never allow cross-tenant messaging.

Never allow AI to send arbitrary bulk messages.

Never send duplicate messages because of retries or duplicate events.

````

## M14 ke baad complete system flow

```text
                    CUSTOMER
                       │
                       ▼
                     TWILIO
                       │
                       ▼
                  M10 VOICE AI
                       │
                       ▼
               M11 TOOL CALLING
                       │
                       ▼
              M11.2 CONTROL LAYER
                       │
          ┌────────────┴────────────┐
          ▼                         ▼
      M12 LEAD                  M13 APPOINTMENT
          │                         │
          │                         ├── Availability
          │                         ├── Booking
          │                         ├── Reschedule
          │                         └── Cancellation
          │                         │
          └────────────┬────────────┘
                       ▼
                DOMAIN EVENTS
                       │
                       ▼
                 M14 COMMUNICATION
                       │
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
   Confirmation      Reminder      Missed Call
        │              │              │
        └──────────────┼──────────────┘
                       ▼
               COMMUNICATION POLICY
                       │
               ┌───────┼────────┐
               ▼       ▼        ▼
            Consent  Quiet    Duplicate
                      Hours      Check
               │       │        │
               └───────┼────────┘
                       ▼
                 TEMPLATE ENGINE
                       │
                       ▼
                  SMS SCHEDULER
                       │
                       ▼
                  TWILIO SMS
                       │
                       ▼
                   CUSTOMER
                       │
                       ▼
              TWILIO STATUS WEBHOOK
                       │
                       ▼
              COMMUNICATION LOG
````

### Example: M13 → M14

Customer ne appointment book ki:

```text
M13
Appointment:
CONFIRMED
Tuesday 2:00 PM
```

M13 event:

```text
appointment.confirmed
```

M14 check karega:

```text
SMS enabled?             ✓
Customer opted out?      ✗
Template available?      ✓
Quiet hours?             ✗
Duplicate?               ✗
```

Phir:

```text
Template
   ↓
"Hi John, your appointment with ABC HVAC is confirmed
for Tuesday at 2:00 PM."
   ↓
Twilio
   ↓
SENT
   ↓
DELIVERED
```

Agar Sunday ko appointment cancel ho gaya:

```text
M13
   ↓
appointment.cancelled
   ↓
M14
   ↓
old reminder invalidated
   ↓
cancellation SMS
```

Isliye **M14 mein communication ko M13 ke andar mix nahi kiya gaya**. Ye separation later debugging, scaling aur provider change ke liye important hai.

**M15 ka logical next phase** ab **Call Logs, Transcripts & Conversation Analytics** hoga—jisme complete call history, transcript, speaker turns, AI/customer messages, call outcome, duration, lead/appointment linkage, searchable call history aur basic AI-generated call summary aayegi.
